node_modules
coverage/
.scannerwork/
graphify-out/
.forgejo-local/
.session/
.workflow/

.sessions/
workflow/.cache/
workflow/.sessions/
workflow/config/agents.local.json
agents.local.json
missions/*/review-events/
# Compiled output (esbuild emits the canonical bundle to build/).
# dist/ stays ignored defensively: TASK-2288 retired that CommonJS tree, and
# scripts/package-content-audit.ts forbids it in a published tarball.
dist/
build/
release-artifacts/
# Stray tsc emit to repo root (noEmit=true, these are dead artifacts)
/index.js
/px.js
/lib/
# tsc incremental build cache
tsconfig.tsbuildinfo
.test-runtime/
.test-runtime.staging-*/
# CodeQL SAST gate (task-2502): databases/results live on $TMPDIR per worktree
# (see scripts/codeql-sast.sh WORK_ROOT), so nothing is tracked.
# StrykerJS scratch/report output (mutation-gate.ts)
.stryker-tmp*/
reports/mutation/

# Canonical-bundle staging/retired trees (atomic swap in scripts/build-canonical-bundle.ts)
.build-staging.*
build.retired.*
/NOTICES
parallix.db*
tmp/
# Agent run logs (older npmtest.out landed here before tmp/ covered it)
.tmp-run/
