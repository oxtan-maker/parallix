node_modules/
coverage/
graphify-out/
.forgejo-local/
.session/
.workflow/

.sessions/
workflow/.cache/
workflow/.sessions/
workflow/config/agents.local.json
agents.local.json
missions/*/review-events/
# Compiled output (tsc/esbuild emit to dist/ and build/)
dist/
build/
release-artifacts/
# Stray tsc emit to repo root (noEmit=true, these are dead artifacts)
index.js
px.js
lib/
# tsc incremental build cache
tsconfig.tsbuildinfo
.test-runtime/
# StrykerJS scratch/report output (mutation-gate.ts)
.stryker-tmp*/
reports/mutation/

# Canonical-bundle staging/retired trees (atomic swap in scripts/build-canonical-bundle.js)
.build-staging.*
.dist-staging.*
build.retired.*
dist.retired.*
/NOTICES
