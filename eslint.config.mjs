import tsPlugin from '@typescript-eslint/eslint-plugin';
import tsParser from '@typescript-eslint/parser';

const ignorePattern = '^_';
const argsIgnorePattern = '^_|^name$';
const caughtErrorsIgnorePattern = '^_';
const varsIgnorePattern = '^_';

const compiledJsIgnores = [
  // Compiled output from TS migration (generated by `npm run build:cjs`)
  'lib/core/*.js',
  'lib/agents/*.js',
  'lib/tools/*.js',
  'lib/review/*.js',
  'lib/commands/*.js',
  'lib/*.js',
  'dist/',
  'node_modules/',
  'graphify-out/',
  '.forgejo-local/',
];

export default [
  // Top-level ignores for directories only (no per-dir *.js globs)
  {
    ignores: [
      'dist/',
      'node_modules/',
      'graphify-out/',
      '.forgejo-local/',
    ],
  },
  // Lint all .ts files, ignoring compiled .js per-directory globs
  {
    files: ['**/*.ts'],
    ignores: compiledJsIgnores,
    languageOptions: {
      parser: tsParser,
      parserOptions: {
        ecmaVersion: 2024,
        sourceType: 'module',
      },
      globals: {
        console: 'readonly',
        process: 'readonly',
        __filename: 'readonly',
        __dirname: 'readonly',
        require: 'readonly',
        module: 'readonly',
        exports: 'readonly',
        Buffer: 'readonly',
        BufferEncoding: 'readonly',
        NodeJS: 'readonly',
        setTimeout: 'readonly',
        clearTimeout: 'readonly',
        setInterval: 'readonly',
        clearInterval: 'readonly',
        URL: 'readonly',
        Headers: 'readonly',
        Request: 'readonly',
        Response: 'readonly',
        fetch: 'readonly',
      },
    },
    plugins: {
      '@typescript-eslint': tsPlugin,
    },
    rules: {
      'no-undef': 'error',
      'no-unused-vars': ['warn', { argsIgnorePattern, varsIgnorePattern, caughtErrorsIgnorePattern }],
      'valid-typeof': 'error',
      'no-unreachable': 'error',
      'no-async-promise-executor': 'error',
      'eqeqeq': 'error',
      'curly': 'error',
      'no-var': 'error',
      '@typescript-eslint/no-require-imports': 'off',
    },
  },
  // Lint remaining .js files (not covered by compiled-js ignores), ignoring compiled .js per-directory globs
  {
    files: ['**/*.js'],
    ignores: compiledJsIgnores,
    languageOptions: {
      parser: tsParser,
      parserOptions: {
        ecmaVersion: 2024,
        sourceType: 'module',
      },
      globals: {
        console: 'readonly',
        process: 'readonly',
        __filename: 'readonly',
        __dirname: 'readonly',
        require: 'readonly',
        module: 'readonly',
        exports: 'readonly',
        Buffer: 'readonly',
        BufferEncoding: 'readonly',
        NodeJS: 'readonly',
        setTimeout: 'readonly',
        clearTimeout: 'readonly',
        setInterval: 'readonly',
        clearInterval: 'readonly',
        URL: 'readonly',
        Headers: 'readonly',
        Request: 'readonly',
        Response: 'readonly',
        fetch: 'readonly',
      },
    },
    plugins: {
      '@typescript-eslint': tsPlugin,
    },
    rules: {
      'no-undef': 'error',
      'no-unused-vars': ['warn', { argsIgnorePattern, varsIgnorePattern, caughtErrorsIgnorePattern }],
      'valid-typeof': 'error',
      'no-unreachable': 'error',
      'no-async-promise-executor': 'error',
      'eqeqeq': 'error',
      'curly': 'error',
      'no-var': 'error',
    },
  },
];
