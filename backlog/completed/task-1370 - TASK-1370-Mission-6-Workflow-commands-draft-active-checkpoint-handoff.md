---
id: TASK-1370
title: 'TASK-1370: Mission 6 - Workflow commands (draft, active, checkpoint, handoff)'
status: done
assignee:
  - custom
created_date: '2026-06-27 10:37'
updated_date: '2026-06-30 03:25'
labels:
  - ai_sdlc
dependencies:
  - TASK-1369
  - TASK-1371
  - TASK-1366
  - TASK-1367
  - TASK-1368
  - TASK-1372
  - TASK-1373
  - TASK-1374
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Convert the 4 main mission lifecycle commands. These are the heaviest command files with the most dependencies across all subsystems.

**Files renamed `.js` → `.ts`:**
- `lib/commands/draft.js` (1002 lines) — create mission branch + worktree. Imports: active, agents, core/*, gitignore, state-map, verification, crypto, fs, path, stats, tools/backlog
- `lib/commands/active.js` (626 lines) — launch agent in mission worktree. Imports: handoff, mission-start, repair-handoff, stats, agents, core/*, review, tools/backlog
- `lib/commands/checkpoint.js` — verify, commit, push checkpoint
- `lib/commands/handoff.js` (573 lines) — sync, push, transition to review. Imports: review/rebase, review/review-state, tools/backlog, forgejo, gatekeeper, setup-review

**Conversion details:**
- Replace `require()` with ES `import` from converted modules
- Replace `module.exports` with ES `export`
- Preserve JSDoc annotations
- Internal command imports must use relative paths (e.g., `import { handoff } from './handoff.js'`)
- `draft.js` and `active.js` are the largest files — pay attention to their complex dependency chains

**No-committed-`.js` guardrails (apply the canonical version in TASK-1364) — directory `lib/commands`:**
- Faithful rename per file: `git diff -M --summary <merge-base> -- lib/commands/X.js lib/commands/X.ts` reports a `rename` ≥ 50%. `draft.js`/`active.js`/`handoff.js` are large — convert in place, do not rewrite; preserve names, helpers, formatting, comments, export shape.
- After converting each file: `git rm --cached lib/commands/X.js` (regenerated by `npm run build:cjs`; no `package.json` change needed).
- `lib/commands` is shared with TASK-1369/1371. Keep `lib/commands/*.js` in `.gitignore` and `.eslintignore` so generated `build:cjs` output stays ignored; do not add command-level negations in this wave, because the remaining hand-written command `.js` files are owned by other missions and should not become a new ESLint blocker here.
- Gates (capture proof): `tsc --noEmit` clean; `npm test` at baseline; `git ls-files lib/commands/{draft,active,checkpoint,handoff}.js` empty; rename shown per file; `grep -c 'module.exports' lib/commands/*.ts` zero; `./scripts/verify-local.sh static-analysis` green **with compiled `lib/commands/*.js` present**; `npm run prepublishOnly && npm pack --dry-run | grep 'lib/commands/'` shows compiled `.js`; `node -e "require('./lib/commands/draft')"` loads with exports intact.

**Dependency:** Depends on TASK-1369 (simple commands), TASK-1371 (integration commands), and all core/agents/review/tools missions.
<!-- SECTION:DESCRIPTION:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Verification gate ran and passed on the final tree with captured proof rather than an unverified claim
- [x] #2 Lint and static analysis report clean on every changed file
- [x] #3 No focused or unannotated skipped tests were introduced (no .only and no bare .skip)
- [x] #4 Final checkpoint Goal Check table cites real evidence using file:line references and test names
- [x] #5 Docs updated to reflect any workflow or user-facing behavior change
- [x] #6 Bug-labeled missions include a red-to-green reproduction test that fails before the fix and passes after
<!-- DOD:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Fixed PR issue: added `lib/commands/*.js` to `.gitignore` and removed build-artifact .js files from tracking. PR now correctly shows 4 renames (R094/R088/R093) instead of both .js and .ts files.
<!-- SECTION:NOTES:END -->
