---
id: TASK-2322.09
title: Cut over SessionMarker persistence and retire worktree session files
status: done
assignee:
  - codex
created_date: '2026-07-29 03:51'
updated_date: '2026-07-30 07:24'
labels:
  - ai_sdlc
dependencies:
  - TASK-2322.02
references:
  - >-
    missions/task-2322.02/CP-3.md (Attempt is excluded; SessionMarker is the
    last resumability marker)
  - src/domain/session.ts
  - src/application/domain-ports.ts
  - src/adapters/sqlite/ports.ts
  - src/adapters/sqlite/session-marker-repository.ts
  - src/adapters/sqlite/session-marker-import.ts
documentation:
  - docs/adr/0053-operational-persistence-and-authority-boundaries.md
modified_files:
  - src/application/consumer-domain-requirements.ts
  - src/application/persistence-domain-map.ts
  - src/application/services/agent-block-service.ts
  - src/domain/README.md
  - src/domain/session.ts
  - src/platform/runtime/lib/core/durable-state-inventory.ts
  - src/platform/runtime/lib/index.ts
  - src/platform/runtime/lib/tools/sessions.ts
  - src/platform/runtime/lib/commands/handoff.ts
  - src/platform/runtime/lib/agents/agents.ts
  - src/platform/runtime/lib/agents/claude.ts
  - src/platform/runtime/lib/agents/codex.ts
  - src/platform/runtime/lib/agents/opencode.ts
  - scripts/build-canonical-bundle.ts
  - scripts/build-sea.ts
  - scripts/package-content-audit.ts
  - docs/npm-package-major-migration.md
  - test/agents.test.ts
  - test/sessions-coverage.test.ts
  - test/sessions.test.ts
  - test/sqlite-adapter-cp1.test.ts
  - test/sqlite-recovery-cp5.test.ts
  - test/task-1124-integrate.test.ts
  - test/task-1416-repro.test.ts
  - test/task-1039-handoff.test.ts
  - test/task-1109.test.ts
  - test/tui-spawn.test.ts
  - test/task-2228-distribution-verification.test.ts
  - test/task-2285-release-metadata.test.ts
  - test/task-2286-native-sea-smoke.test.ts
  - missions/task-2322.09/CP-7.md
parent_task_id: TASK-2322
priority: high
ordinal: 9000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Make the checked SessionMarker repository the sole live authority for resumable agent sessions. Move all create, lookup, resume, replace, and clear behavior behind application ports, import any needed worktree markers explicitly, then remove routine reads and writes under .workflow/sessions. Session identity must survive worktree cleanup and must not be confused with an inferred Attempt aggregate.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 All commands and agent-launch paths create, query, replace, and clear SessionMarker values through a checked application port and the operator database.
- [x] #2 Legacy worktree session discovery has dry-run and atomic idempotent import behavior, reports conflicts without overwriting either side, and leaves source files untouched.
- [x] #3 After cutover no production path probes, reads, writes, deletes, or falls back to .workflow/sessions; database failure produces an explicit operation failure.
- [x] #4 SessionMarker identity, repository and mission association, role, provider session identifier, and replacement semantics round trip and obey checked invariants.
- [x] #5 Worktree cleanup or movement does not erase or change the authoritative session marker, while explicit clear behavior remains observable and tested.
- [x] #6 Resume, failover, duplicate marker, stale update, restart, and database-unavailable behavior is covered with fast mocked provider dependencies and isolated database fixtures.
- [x] #7 No Attempt table or adapter record is introduced unless TASK-2322.02 first added the checked domain concept and ADR decision.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Recovered from the approved mission/checkpoint record for final startup repair:
1. Reproduce and localize the migration checksum mismatch without weakening immutable-migration validation.
2. Compare the current 0001 SQL bytes and checksum against Git history and the operator database ledger to identify whether code or local persisted state drifted.
3. Apply the smallest in-scope corrective change, preserving forward-only migrations and explicit database failure behavior.
4. Run focused migration/session-marker tests plus required static-analysis and general verification gates.
5. Refresh graphify output if code changes, update checkpoint/task evidence, and retry `active --implementer codex`.

Operator approved folding the pre-existing AgentBlockService ESLint repair into this task so the required final gates can pass.

Review-transition defense repair authorized by operator: (1) reproduce the real PTY smoke failure repeatedly and trace Enter handling/confirmation rendering; (2) distinguish timing flake from product regression without weakening the assertion; (3) apply the smallest correction that preserves guarded consequential actions and prevents agent launch; (4) run the targeted PTY test, relevant fast tests, required verification gates, and graphify update after code changes.

Approved review-repair plan (2026-07-29):
1. Normalize runtime workflow identities at the launch boundary: implementer -> execute and reviewer -> review, while accepting already-canonical execute/draft/review values.
2. Remove unsafe SessionRole casts from lookup/save/delete calls and fail explicitly for unsupported runtime roles.
3. Resolve one checked SessionMarkerPort per launch attempt and forward that same port plus the canonical role into provider launchers so stale-session deletion uses production wiring.
4. Add fast mocked-launcher regressions for canonical persistence and provider stale-session port threading; keep all database coverage isolated.
5. Run focused agent/session tests, static-analysis, general verification, graphify update, and refresh CP-7/task evidence before returning to review.

Defense-suite repair continuation authorized by the operator: reproduce the three reported failures, identify any unmocked SessionMarker/Forgejo/status dependencies introduced by the mission, patch only source-of-truth runtime or isolated test fakes as appropriate, run focused tests plus required static-analysis/general verification, and refresh graphify after code changes.

Executed defense repair: isolate handoff/integrate fixtures by process id to prevent concurrent verifier collisions; preserve skipGate/forceWithLease across recursive handoff remediation; run the shipped status smoke test from its disposable fixture so it verifies the headless artifact without traversing live mission state.

Operator correction for the reported defense failures: do not expand or add assertions around the transitional CommonJS `.test-runtime`; CommonJS retirement remains the repository direction. Repair this mission at the production source boundary by keeping SessionMarker identities canonical in the launch coordinator, passing those typed identities to provider launchers, and relying on the checked SessionMarkerPort/adapter for persistence validation. Run the reported repro set and existing mission gates without adding a new CommonJS compatibility surface.

2026-07-30 integration defense repair: reproduce the real custom-agent smoke `no such table: session_markers` failure; trace operator-database initialization and migration ordering without weakening the smoke assertion; apply the smallest production or isolated-fixture correction that preserves SessionMarker authority and explicit database failures; add fast isolated regression coverage; run focused tests, static-analysis/general verification, the custom-agent smoke when feasible, and `graphify update .` after code changes.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-07-29 startup repair: `active --implementer codex` failed before launch because `<PARALLIX_HOME>/parallix.db` contained legacy migration 0001 checksum `89ddae…` from 2026-07-21, while the canonical SQL hashes to `30a76f…`. The legacy DB was healthy but empty (0 blocklist, usage, and import rows), so it was preserved as `/home/magnus/.local/state/parallix/parallix.db.pre-checksum-repair-20260729T1055Z`; a fresh DB was initialized through `initOperatorState` with migrations 0001–0004. Read-only integrity check returned `ok` and all nine expected tables exist.

Focused verification: `./node_modules/.bin/tsx --test test/sqlite-adapter-cp1.test.ts test/session-marker-repository.test.ts` passes 48/48 after updating the stale current-version assertion from 0003 to 0004. `graphify update .` rebuilt graphify-out successfully.

Scope blocker: `./scripts/verify-local.sh static-analysis` fails in pre-existing `src/application/services/agent-block-service.ts:24` (`repository` reported unused by ESLint). The file belongs to commit 3b1a986c3d / Task 1327 and is unrelated to SessionMarker cutover; no change made pending operator scope decision.

Scope decision: operator approved fixing the unrelated mechanical ESLint failure in the current task.

Final verification after guardrail repairs: `./scripts/verify-local.sh static-analysis` passed all four stages; `./scripts/verify-local.sh all` passed 1,504/1,504 tests. CP-7 records file:line and test evidence.

Local checkpoint committed via `npm run dev -- checkpoint task-2322.09 CP-7 ...`. Task remains active intentionally so the normal Parallix handoff/review lifecycle can perform the status transition.

Review-transition defense repair: the reported PTY confirmation failure passed five isolated reruns, the exact integration suite, and the final general verifier, so it was classified as a transient input/render scheduling flake and neither production TUI behavior nor the assertion was changed. The exact integration rerun exposed five deterministic SessionMarker regressions behind it: four `startAgent` tests still passed the retired `sessionsModule` fake, causing the default checked adapter to load and fail through the incomplete CommonJS test-runtime graph, and `sqlite-recovery-cp5` hard-coded the pre-0004 migration count. Replaced the four launch fakes with isolated `SessionMarkerPort` fakes, removed four dead `sessionsModule` options from task-1416 fixtures, and made the recovery assertion follow `migrations.length`. Did not expand the retired CommonJS test runtime; follow-up ESM-only cleanup is tracked separately as TASK-2323. Evidence: focused 109 passed / 1 annotated skip / 0 failed; static-analysis all four stages passed; `npm run test:integration` 1,416 total / 1,391 passed / 25 annotated skips / 0 failed; `./scripts/verify-local.sh all` 1,495/1,495 passed; `graphify update .` completed.

2026-07-29 review-repair diagnosis: reproduced the production role mismatch. The checked domain/migration accepts SessionRole execute|draft|review, but runtime callers pass implementer|reviewer through `as any`; the shared operator DB rejects reviewer after the reviewer process exits successfully. The launch coordinator resolves the production SessionMarkerPort for lookup/save but does not forward it into provider launchers, so stale-session cleanup cannot use the production port.

Read-only operator DB audit: `0004-session-markers` was applied at 11:21 and created the rejecting CHECK; TASK-2322.03's separate `0004-mission-aggregate` was applied later at 14:54. Therefore TASK-2322.03 did not cause this failure. The DB is nevertheless shared globally under `~/.local/state/parallix/parallix.db` across worktrees.

2026-07-29 review repair implemented: `startAgent` now maps implementer->execute and reviewer->review at the runtime/application boundary, accepts already-canonical roles, rejects unsupported roles before launch, reuses one checked SessionMarkerPort for lookup/save, and forwards that same port plus canonical role into provider launchers for stale-session deletion. No migration bytes or restricted domain types changed.

2026-07-29 persistence-type repair: made `session_markers` a strict, repository-scoped table keyed by `(repository_id, mission_id, role)` so identical mission slugs in different repositories cannot collide in the shared operator database. The SQLite repository is constructed with a checked `RepositoryId`; persisted mission, role, and agent fields now retain `MissionId`, `SessionRole`, and `AgentFamily` types and are validated when decoded. Save input no longer pretends to own database-generated `updated_at`, nullable session IDs round-trip without truthiness coercion, and `MissionId` now accepts dotted child-task IDs such as `task-2322.09`.

Verification: focused mocked-provider and isolated-SQLite suite passed 194 tests with 1 annotated skip and 0 failures; `./scripts/verify-local.sh static-analysis` passed all four stages; `./scripts/verify-local.sh all` passed; `graphify update .` rebuilt the code graph. CP-7 now cites the repair and regression evidence.

First post-repair handoff rebased cleanly onto latest main, then correctly stopped before Forgejo push because main's full gate found six stale source-citation anchors/line numbers and one string guardrail still naming the old local `port`. Updated `consumer-domain-requirements.ts` to the canonical `launchSessionMarkerPort` anchors and rebased line locations, and updated TASK-1124's guardrail expectation. Focused citation/guardrail tests pass 19/19; static analysis passes; graphify refreshed.

Operator reported three defense failures after the SessionMarker review repair: gatekeeper pushback, Variant B sync-failure diagnostics, and build/px.mjs status timeout. Investigation started from graphify and focused source/test inspection.

Defense repair complete. Root causes were fixed `/tmp` fixture paths colliding across concurrent verifier processes, recursive handoff remediation dropping caller gate options, and the shipped status smoke test running against the live mission checkout (15–30s under load). Focused canonical run passed 26/26; status smoke fell to ~0.52s. `./scripts/verify-local.sh static-analysis` passed all four stages. `./scripts/verify-local.sh all` passed 1,512/1,512 tests; full-suite status smoke completed in ~0.59s. `git diff --check` passed and `graphify update .` rebuilt 14,124 nodes / 16,199 edges.

2026-07-29 final defense repair: the reported MODULE_NOT_FOUND failures were caused by new runtime domain-constructor imports from the four agent launcher modules crossing the incomplete transitional `.test-runtime` graph. Per operator correction, did not expand the CommonJS builder. The coordinator now retains already-selected mission/agent identities as branded values, provider launcher options require MissionId/SessionRole, and checked SQLite repository validation remains authoritative. Rebased consumer/invariant citation lines and refreshed CP-7 plus graphify. Evidence: reported repro set 64/64; launcher/SessionMarker set 206 passed with 1 existing annotated skip; citation set 34/34; static-analysis all four stages passed; `verify-local.sh all` 1,464/1,464 with 0 skips; graphify 14,593 nodes / 17,022 edges.

2026-07-30 reviewer-launch checksum repair: audited shared operator DB read-only. 0004-mission-aggregate ledger held intermediate checksum 53696be6… while canonical main/mission SQL hashes to 27a88fa4…; the schemas are materially different, so a ledger-only edit was rejected as unsafe. The legacy missions table had 0 rows; live state comprised 3 agent-block rows, 7 board-lane events, and 2 authoritative SessionMarker rows. Built a fresh database through the canonical migration runner, copied all compatible live rows, verified row-count parity and PRAGMA integrity_check = ok, then replaced the operator DB after preserving /home/magnus/.local/state/parallix/parallix.db.pre-checksum-repair-20260729T232728Z. Real initOperatorState now succeeds with canonical checksums and both SessionMarkers intact. px review task-2322.09 --continue advanced past database initialization, proving the checksum launch blocker is resolved, but latest-main rebase now exposes separate shared conflicts (src/application/domain-ports.ts, later docs/test conflicts). The attempted automatic rebase was aborted; mission branch restored clean at 900a6d1d8 with no source edits.

2026-07-30 verification-gate repair: fresh SQLite fixtures failed because immutable `0005-repository-scoped-session-markers` sorted before the rebased `0006-session-markers` table-creation migration. Preserved both SQL files and checksums, and made `loadDefaultMigrations()` enforce the historical dependency while retaining normal lexical order elsewhere. Added regressions for fresh ordering, unscoped-row upgrade preservation, and an already-scoped operator ledger with 0006 pending; removed the stale unused `MissionStatus` import blocking ESLint. Evidence: reported/focused SQLite set 84/84 passed; `./scripts/verify-local.sh static-analysis` passed all four stages; `./scripts/verify-local.sh all` passed 1,574/1,574 with 0 failed and 0 skipped; `git diff --check` passed; `graphify update .` rebuilt 14,900 nodes / 17,660 edges. Existing operator database was inspected read-only only; its migration ledger and two SessionMarker rows were not modified.

Integration returned TASK-2322.09 to active after `px active --implementer custom` failed in the real-agent smoke because the launched process opened a database without a `session_markers` table.

2026-07-30 integration defense root cause and repair: the canonical ESM bundle retained filesystem-based migration loading but did not stage `src/adapters/sqlite/migrations` under `build/`. The real-agent smoke's fresh isolated PARALLIX_HOME therefore opened an empty database and failed its first checked SessionMarker lookup. Staged the immutable SQL files through the canonical runtime asset/checksum manifest, made package auditing require them, removed the SEA-only duplicate copy, updated distribution documentation, and converted the existing documented-divergence assertion into schema-presence coverage. No persistence fallback or database-error suppression was added.

Verification after bundle repair: focused packaging/SQLite/SessionMarker suite 76/76; static-analysis passed all four stages; `./scripts/verify-local.sh all` passed 1,561/1,561 with zero skips; `npm run test:package-content` passed for 25 checksummed files; exact `node --import tsx test/e2e-real-agent-smoke.test.ts` passed the custom/Pi full lifecycle (draft 43,910 ms, active 174,190 ms); `git diff --check` passed; `graphify update .` rebuilt 14,909 nodes / 17,667 edges.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
author: codex
created: 2026-07-29 14:52
---
Post-handoff reconciliation: the normal handoff committed `review` on the integration base and updated Forgejo PR #193, but the mission branch replay retained its earlier `active` task copy. Reconciled the mission task copy to the already-authoritative review state so status projections and the review branch agree.
---

author: codex
created: 2026-07-29 15:10
---
Reviewer launch failure confirmed; task moved back to active for repair. No dev-DB-isolation follow-up was created under the operator's stated condition because the migration ledger disproves TASK-2322.03 as the cause: session-markers preceded mission-aggregate by over three hours.
---
<!-- COMMENTS:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Final defense repair for TASK-2322.09 removes the runtime domain-constructor edges that broke the transitional test graph after SessionMarker cutover, without adding CommonJS compatibility code or weakening production persistence. The launch coordinator keeps mission, role, and agent identities canonical; Claude, Codex, and OpenCode stale-session paths accept and forward typed MissionId/SessionRole values to the same checked SessionMarkerPort. Checked SQLite validation, database-unavailable failure behavior, resume/failover semantics, and provider isolation remain covered. Consumer and persistence citation maps and CP-7 evidence were refreshed. Verification: 64 reported repro assertions passed; 206 launcher/SessionMarker assertions passed with one existing annotated skip; 34 citation assertions passed; static analysis passed all stages; full fast verifier passed 1,464/1,464 with no skips; graphify update completed.

Integration defense repair: fixed the canonical npm bundle so fresh operator databases receive the complete immutable migration set before SessionMarker access. SQLite migrations are now staged and checksummed as canonical runtime assets shared by npm and SEA; package auditing prevents future omission. The real custom-agent full-lifecycle smoke now passes without weakening SessionMarker authority, explicit database failures, launcher assertions, or migration checksum validation. Final evidence: focused 76/76, static analysis clean, fast verifier 1,561/1,561, package-content audit clean, and exact custom/Pi smoke passed.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Verification gate ran and passed on the final tree with captured proof rather than an unverified claim
- [x] #2 Lint and static analysis report clean on every changed file
- [x] #3 No focused or unannotated skipped tests were introduced (no .only and no bare .skip)
- [x] #4 Final checkpoint Goal Check table cites real evidence using file:line references and test names
- [x] #5 Docs updated to reflect any workflow or user-facing behavior change
- [x] #6 Bug-labeled missions include a red-to-green reproduction test that fails before the fix and passes after
<!-- DOD:END -->
