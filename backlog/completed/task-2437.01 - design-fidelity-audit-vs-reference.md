---
id: TASK-2437.01
title: Design-fidelity audit of the web board against the reference design
status: done
assignee: [custom]
labels:
  - web
  - board
  - design-fidelity
  - user_value
dependencies:
  - TASK-2437
priority: medium
---

## Description

CP-8 of TASK-2437 could not complete its design-fidelity gate because the
reference export `/tmp/Parallix Kanban Board Controller.zip` was absent from
this worktree when the task was created. `Parallix Board GPU.dc.html` is the
reference screen within that export; the archive contains other designs that
are not the comparison baseline. Per TASK-2437 stop rules and its
"Risks and Assumptions" ("if absent, treat design-fidelity fixes as follow-ups
rather than inventing requirements"), the full audit is deferred here.

Known-item check already performed (no zip required): the repository name is
an approved divergence. `web/src/top-bar.tsx` renders the product name
`Parallix` plus the live `snapshot.repositoryId` (for example, `Parallix` for
that repository); `Parallix Board GPU.dc.html` has no repository-name field.
This is not an extra product-name token and must remain during the audit.

## Acceptance Criteria

- [ ] #1 Obtain `/tmp/Parallix Kanban Board Controller.zip` (or an approved
      equivalent export) into the worktree and use `Parallix Board GPU.dc.html`.
- [ ] #2 Audit every web surface (`web/src/*`) against the reference for missing
      elements; fix only defects in the web board slice, no redesign.
- [ ] #3 Retain the approved repository-identity divergence: the product name
      renders once and the live repository identity renders separately, even
      though the GPU reference has no repository-name field.
- [ ] #4 Any unmet reference element becomes a scoped fix, never a waiver.
