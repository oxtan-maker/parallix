---
id: TASK-2455.03
title: enforce workflow config schema validation
status: done
assignee: [custom]
created_date: '2026-09-05 10:00'
labels: [bug, ai_sdlc]
dependencies: []
---

## Description

`px config` calls `validateWorkflowConfig`, but that validator only rejects an
invalid top-level object, invalid adapter containers, and an invalid positive
integer for `adapters.agents.maxConcurrentCustom`. It does not enforce the
field-level types and enums declared by `config/workflow.config.schema.json`.

Reproduce in a temporary repository with:

```json
{ "adapters": { "review": { "provider": "unsupported" } } }
```

The command currently exits zero and prints an effective configuration, despite
the schema enum allowing only `forgejo`, `none`, or `null`. Make `px config`
exit non-zero with a structural-validation diagnostic for this and comparable
schema violations, while retaining the fallback-default output for invalid
files.

## Acceptance Criteria

- [ ] A regression test named for an enum or type violation proves `px config`
      exits non-zero and reports a structural-validation diagnostic.
- [ ] Validation covers every documented schema field or the schema explicitly
      narrows to the validation that the runtime enforces.

## Implementation (task-2455.03)

`validateWorkflowConfig` in `src/adapters/config/product-config.ts` now enforces
every constraint in `config/workflow.config.schema.json`: object types for
`product` and each adapter section; string fields across `tasks`/`missions`/
`verification`/`integrate`/`review`; `tasks.storage` string-or-object; nullable
review provider restricted to `forgejo`, `none`, or `null`; positive-integer
`maxConcurrentCustom`; string-valued `agents.models`; the closed,
enum-restricted `runners.custom`; and the closed, nullable non-negative
`subagents.maxParallel`. Unknown keys stay allowed where the schema sets
`additionalProperties: true`.

Evidence:
- `test/config-command.test.ts` test `config rejects an unsupported review provider enum value as fallback defaults and exits non-zero` (regression; red at parent commit `302bf57d0`, green after fix `4bf7fa38c`).
- `test/product-config.test.ts` per-constraint coverage; suite 50/50.
- `npx tsc --noEmit` clean.
- Note: `./scripts/verify-local.sh all` is blocked by three pre-existing failures on clean `main` (`BoardProjectionBuilder`, `performStaticReview ...spaces`, `web-board-interaction`), unrelated to this scope. See `missions/task-2455.03/CP-3.md`.
