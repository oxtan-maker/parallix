---
id: TASK-2369.17
title: Dedup classifyHookFailure/handleHookFailureAutoBounce between integrate and rebase
status: active
assignee: [custom]
created_date: '2026-08-12'
labels: [ai_sdlc]
dependencies: [TASK-2369.06]
---

## Description

`classifyHookFailure()` and `handleHookFailureAutoBounce()` exist in both:

- `src/adapters/cli/commands/integrate.ts` — adapter-side
- `src/application/rebase-workflow.ts` — application-side (canonical, uses port)

The application version is canonical per ADR 0051 (application owns workflow policy). Extract this to a seperate file and make `integrate.ts` and `rebase-workflow.ts` import that and remove the duplicate definitions. After task-2369.06 extracts post-integration helpers, the integrate file is smaller and the import is clean.

## Files

- `src/application/rebase-workflow.ts` — canonical source
- `src/adapters/cli/commands/integrate.ts` (or `integrate-post.ts` after .06) — remove duplicates, import

## Acceptance Criteria

- Zero duplicate code for these 2 functions
- `./scripts/verify-local.sh static-analysis` passes
