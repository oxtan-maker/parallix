---
id: TASK-2521.07
title: >-
  Mission 7 — Remove retired Mission and Backlog metadata from the current
  repository tree
status: backlog
assignee: []
created_date: '2026-09-16 09:10'
labels:
  - migration
  - workflow
dependencies:
  - TASK-2521.06
references:
  - docs/adr/0053-operational-persistence-and-authority-boundaries.md
parent_task_id: TASK-2521
priority: high
ordinal: 79007
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Part of wave TASK-2521 (read its architectural constraints and mandatory agent-slop checks; they apply here).

Depends on: Mission 6 (TASK-2521.06) GO result. MUST NOT start until Mission 6 reports zero unresolved legacy material and zero unexplained normal-runtime readers/writers of the paths being removed.

Purpose: make the current product repository compact and readable.

### Work

Only after the zero-unresolved gate passes:

* remove historical `missions/**` workflow material from the current tree;
* remove Parallix-self-hosted task artifacts under:
  * `backlog/tasks/**`;
  * `backlog/completed/**`;
  * `backlog/archive/**`;
* remove obsolete `backlog.md` material when its unique information has been migrated;
* remove retired review/NEL/checkpoint compatibility files;
* remove obsolete code, parsers and path utilities that exist solely for normal-runtime access to those deleted representations;
* retain explicit legacy import code only where continued import of external/older repositories is a supported product requirement;
* retain genuine product/configuration files even if they happen to live near retired metadata;
* remove obsolete tests that assert old file layout and replace them with behavioral tests;
* do not rewrite Git history.

Do NOT simply `rm -rf backlog missions` before examining every surviving file. An unexpected file in either tree is a STOP condition requiring classification.

Add a regression guard that fails if normal Parallix self-hosting recreates the retired workflow-metadata tree.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 `missions/**` contains no retained historical workflow ledger in the current tree.
- [ ] #2 Parallix's own `backlog/tasks`, `backlog/completed`, `backlog/archive`, and obsolete `backlog.md` task state are removed from the current tree.
- [ ] #3 Any surviving file under those roots has a demonstrated product/config/external-provider reason to exist.
- [ ] #4 Git history is untouched.
- [ ] #5 Build/tests do not rely on restoring the deleted metadata.
- [ ] #6 No normal Mission run recreates those directories/files.
- [ ] #7 Repository file-count/footprint reduction is reported before/after as command output/checkpoint evidence, not as a new committed report file.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 Verification gate ran and passed on the final tree with captured proof rather than an unverified claim
- [ ] #2 Lint and static analysis report clean on every changed file
- [ ] #3 No focused or unannotated skipped tests were introduced (no .only and no bare .skip)
- [ ] #4 Final checkpoint Goal Check table cites real evidence using file:line references and test names
- [ ] #5 Docs updated to reflect any workflow or user-facing behavior change
- [ ] #6 Bug-labeled missions include a red-to-green reproduction test that fails before the fix and passes after
<!-- DOD:END -->
