---
id: TASK-2369.01
title: Remove CSV import from stats.ts
status: ready-for-integration
assignee: [codex]
created_date: '2026-08-12'
labels: [ai_sdlc]
dependencies: []
---

## Description

CSV import functionality is no longer needed. ADR 0053 made SQLite the sole statistics authority; `stats.csv` is an explicit, operator-invoked, read-only import only. Remove all CSV-related code from `src/adapters/cli/commands/stats.ts`.

Remove: `parseCsvLine`, `loadCsv`, `readLegacyStatsCsv`, `LEGACY_HEADERS`, `analyzeLegacyStatsCsv`, `applyLegacyStatsCsv`, `runLegacyCsvImportCommand`, `IMPORT_DATE_RE`, and `statsRepoIdentities`/`legacyStatsRepoAliases` (only exist for CSV repo matching).

Also remove `@ts-nocheck` — file drops below 2000 lines, enabling `--checkJs`.

## Files

- `src/adapters/cli/commands/stats.ts` — delete CSV functions and constants
- Any caller of removed functions (grep for `readLegacyStatsCsv`, `analyzeLegacyStatsCsv`, `runLegacyCsvImportCommand`, `legacyStatsRepoAliases`, `statsRepoIdentities`)

## Acceptance Criteria

- `./scripts/verify-local.sh static-analysis` passes
- No runtime path calls removed functions
- `@ts-nocheck` removed, no new type errors
