---
id: TASK-2369.13
title: Dedup goal-check evidence helpers between handoff and review
status: review
assignee: [custom]
created_date: '2026-08-12'
labels: [ai_sdlc]
dependencies: [TASK-2369.08]
---

## Description

Goal-check evidence helpers exist in two places with near-identical logic:

- `src/adapters/review/review-commands.ts` — adapter-side (uses `node:fs` directly)
- `src/application/handoff-command-use-case.ts` — application-side (uses `fileSystem` port)

After task-2369.08 extracts these to `review-static-evidence.ts`, make `handoff-command-use-case.ts` import from the shared module. The adapter version is canonical; the handoff use case already adapts its port to match the adapter signature.

Functions: `collectGoalCheckEvidenceRows()`, `collectRepoTestNames()`, `canonicalSourceContainsFile()`, `evidenceCellHasVerifiableReference()`, `findUnverifiableGoalCheckRow()`.

## Files

- `src/adapters/review/review-static-evidence.ts` — canonical (after .08)
- `src/application/handoff-command-use-case.ts` — remove duplicates, import from review-static-evidence

## Acceptance Criteria

- Zero duplicate code for these 5 functions
- handoff-command-use-case.ts drops by ~160 lines
- `./scripts/verify-local.sh static-analysis` passes
