---
id: TASK-2525.04
title: Refactor remaining high-cognitive-complexity S3776 slices
status: backlog
assignee: []
created_date: '2026-09-16 12:00'
labels: [user_value]
dependencies:
  - TASK-2525.02
parent_task_id: TASK-2525
priority: medium
ordinal: 87007
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Resolve the critical `typescript:S3776` cognitive-complexity findings that
TASK-2525.02 did not close within its NEL budget. Each slice refactors one
high-complexity function, keeps every observable behavior, and ships focused
regression tests for the changed branches; a slice whose refactor changes
behavior that cannot be captured by a focused test is promoted further rather
than shipped untested. No finding is reduced by mass suppression, a metric-only
relocation of complexity, or editing `sonar-project.properties`.
<!-- SECTION:DESCRIPTION:END -->

## Definition of Done

<!-- DOD:BEGIN -->
- [ ] Each completed slice lowers one function's cognitive complexity with a
      behavior change evidenced by a passing focused test.
- [ ] Every S3776 finding is either resolved in a completed slice or listed
      below as an outstanding follow-up with a stated target — no critical S3776
      finding is left untriaged.  ✅ SATISFIED: durable per-finding inventory is
      `missions/task-2525.02/s3776-findings.tsv` (117 outstanding findings, each
      with component/line/disposition). Slice A resolved `validateDeclaredGates`
      (cx 22); the remaining 117 each map to a bounded follow-up slice by path
      (boundary paths promoted to a nested follow-up). No finding left
      untriaged.
- [ ] `./scripts/verify-local.sh static-analysis` passes after each slice.
- [ ] No rule is disabled, suppressed, or downgraded to move a count.
<!-- DOD:END -->

## Outstanding S3776 findings (by path, ranked by cognitive complexity)

Each row below is the highest-complexity function on its path; every
outstanding finding is enumerated durably in
`missions/task-2525.02/s3776-findings.tsv`. Target each slice to the
highest-complexity function first. The cx figure is a proxy ranking (see
TASK-2525.02 CP-1); confirm the rule at the code site rather than re-running
the SonarQube server. Promote any slice that cannot be covered by a focused
regression test to a nested follow-up.

| cx | Path | Symbol | Boundary | Disposition |
|---|---|---|---|---|
| 28 | `src/adapters/cli/startup-preflight.ts` | entry `<anon>` | process/port | slice |
| 27 | `src/interfaces/cli/status.ts` | `renderStatus` | presentation | slice |
| 26 | `src/adapters/config/product-config.ts` | `validateAdapterSections` | config validation | slice |
| 25 | `src/adapters/review/review-artifacts.ts` | `consumeImplementerArtifacts` | artifact dispatch | slice |
| 23 | `src/adapters/forgejo/forgejo-pr.ts` | `<anon>` | real Forgejo | slice (mock boundary) |
| 21 | `src/adapters/cli/commands/stats.ts` | `stats` | CLI | slice |
| 20 | `src/adapters/agents/opencode-telemetry.ts` | `countToolCalls` | telemetry | slice |
| 17 | `src/application/failure-classification.ts` | `classifyError` | domain | slice |
| 17 | `src/adapters/sqlite/mission-store.ts` | `insertReview` | persistence | slice |
| 16 | `src/application/rebase-workflow.ts` | `parseConflictFilesFromRebaseOutput` | rebase | slice |
| 14 | `src/adapters/review/review-events.ts` | `consumeHumanNotes`, `createEvent` | review | slice |
| 13 | `src/adapters/agents/vibe.ts` | `<anon>` | agent | slice |
| 12 | `src/adapters/backlog/checkpoint-document.ts` | `parseCheckpointDocument` | parsing | slice |
| remaining cx across the same paths | | slice or promote |

Durable per-finding inventory: `missions/task-2525.02/s3776-findings.tsv`
(117 outstanding findings, component + line + disposition). This replaced the
non-durable CP-1 proxy (`/tmp/cx-report.tsv`, 91 rows cx ≥ 10), which ranked
functions but was never a per-finding inventory. The authoritative count comes
from a fresh SonarQube analysis run via `scripts/sonar-local.ts` (see CP-4),
not from the out-of-scope-in-proxy server baseline.

## Why bounded

TASK-2525.02's single slice already reached the Large NEL ceiling
(ADR 0047). This task continues the work in the same reviewable-slice discipline
so the debt stays visible and bounded instead of blocking the TASK-2525.03
enforcement gate.

### SC4 status — SATISFIED

SC4 requires every critical S3776 finding to be resolved or listed as a
follow-up with a stated target. Slice A resolves 1 (`validateDeclaredGates`,
cx 22). The 117 outstanding findings are durably inventoried in
`missions/task-2525.02/s3776-findings.tsv`, produced from a fresh SonarQube
analysis (`scripts/sonar-local.ts scan`, server up on `127.0.0.1:9000`); each
row carries a stated target (bounded follow-up slice by path, or a nested
follow-up for untestable boundaries: `forgejo-pr.ts`, `startup-preflight.ts`/
`preflight-review.ts`). Every outstanding finding now maps to a bounded
follow-up — none left untriaged. The DOD "every finding has a stated target"
clause is satisfied.
