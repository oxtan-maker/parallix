---
id: TASK-2437.01
title: Design-fidelity audit of the web board against the reference design
status: backlog
assignee: [custom]
labels:
  - web
  - board
  - design-fidelity
  - user_value
dependencies:
  - TASK-2437
priority: medium
---

## Description

CP-8 of TASK-2437 could not complete its design-fidelity gate because the
reference design `/tmp/Parallix Kanban Board Controller.zip` is absent from this
worktree (verified: file not present). Per TASK-2437 stop rules and its
"Risks and Assumptions" ("if absent, treat design-fidelity fixes as follow-ups
rather than inventing requirements"), the full audit is deferred here.

Known-item check already performed (no zip required): the hallucinated extra
called out in TASK-2437 #14 — "Parallels displayed twice, once for product name
and once for repo name" — is ABSENT in the current web board. `web/src/top-bar.tsx`
renders the product name `Parallix` plus the live `snapshot.repositoryId` (the
actual working-tree repo), not a duplicated `Parallels` token. `grep -rniE
"parall[sx]|Parallels" web/src/` finds only comment headers and that single
top-bar product label.

## Acceptance Criteria

- [ ] #1 Obtain `/tmp/Parallix Kanban Board Controller.zip` (or an approved
      reference export) into the worktree.
- [ ] #2 Audit every web surface (`web/src/*`) against the reference for missing
      elements; fix only defects in the web board slice, no redesign.
- [ ] #3 Confirm no hallucinated extras remain (Parallels rendered once as the
      product name, the repo identity from the live projection, not a duplicate).
- [ ] #4 Any unmet reference element becomes a scoped fix, never a waiver.
