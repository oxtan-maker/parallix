---
id: TASK-2369.11
title: Split forgejo.ts — extract auth and token helpers
status: backlog
assignee: [custom]
created_date: '2026-08-12'
labels: [ai_sdlc]
dependencies: []
---

## Description

`src/adapters/forgejo/forgejo.ts` (1700 lines) is a god adapter. Extract auth, token resolution, and Forgejo home/settings helpers into `src/adapters/forgejo/forgejo-auth.ts`.

Extract: `resolveForgejoUser()`, `DEFAULT_FORGEJO_USER`, `resolveForgejoHome()`, `normalizePathForComparison()`, `isForgejoPath()`, `resolveForgejoSettings()`, `resolveForgejoAuth()`, `resolveTokenFile()`, `readToken()`, `cacheKey()`, `deriveRepoFromGitRemote()`, `listGitWorktrees()`, `forgejoAvailable()`, `DISPOSITION_PATTERN`.

## Files

- `src/adapters/forgejo/forgejo.ts` — remove extracted functions, re-export
- `src/adapters/forgejo/forgejo-auth.ts` — new file

## Acceptance Criteria

- forgejo.ts drops by ~300 lines
- `./scripts/verify-local.sh static-analysis` passes
