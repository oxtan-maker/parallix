---
id: TASK-2521.01
title: >-
  Mission 1 — Lock the landed ADR contract and add executable anti-regression
  guardrails
status: backlog
assignee: []
created_date: '2026-09-16 09:09'
labels:
  - architecture
  - persistence
  - trust-model
dependencies:
  - TASK-2511
references:
  - docs/adr/0032-mission-refinement-state-and-usage-budget-signals.md
  - docs/adr/0036-mission-sizing-and-dependency-wave-heuristics.md
  - docs/adr/0037-ai-workflow-coordination-architecture.md
  - docs/adr/0047-per-mission-change-size-budget.md
  - docs/adr/0048-fail-closed-harness-defense-against-agent-hallucinations.md
  - docs/adr/0051-ui-neutral-application-boundary.md
  - docs/adr/0053-operational-persistence-and-authority-boundaries.md
parent_task_id: TASK-2521
priority: high
ordinal: 79001
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Part of wave TASK-2521 (read its architectural constraints and mandatory agent-slop checks; they apply here).

Depends on: TASK-2511 ADR changes landed on the branch being implemented.

Purpose: establish exactly what the current architecture says and identify every production dependency on repository workflow metadata before changing persistence.

### Work

* Re-read the CURRENT versions of ADR 0032, 0036, 0037, 0047, 0048, 0051 and 0053 from the implementation branch.
* Treat those files, not this task's paraphrase, as architecture authority.
* If the landed ADRs materially contradict this wave's assumptions, STOP and report the contradiction instead of "reconciling" it in code.
* Identify every production read and write of:
  * `missions/**`;
  * `MISSION.md`;
  * `CP-*.md`;
  * review-state/review-event/NEL workflow files;
  * `backlog.md`;
  * `backlog/tasks/**`;
  * `backlog/completed/**`;
  * `backlog/archive/**`;
  * task-file fallback summaries;
  * any other machine-generated workflow metadata discovered during the audit.
* Classify each call site as one of:
  * current DB-owned state that must become application/DB native;
  * external/task-provider input;
  * explicit legacy importer;
  * explicit user-requested export;
  * ephemeral transport;
  * product/configuration file;
  * obsolete path to remove.
* Do NOT create a prose inventory file. Encode stable architectural restrictions in tests/guards where useful; use checkpoint evidence for the one-time audit.
* Add a guard that makes introducing a new normal-runtime write to retired workflow paths fail tests unless the call site is explicitly an import/export/test fixture.
* Add a guard against new application/interface code treating `missions/**` or repo Backlog task files as Mission persistence.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Every current production read/write of the targeted paths is accounted for with file:symbol evidence.
- [ ] #2 No unidentified normal-runtime persistence path remains.
- [ ] #3 Architecture tests fail on a newly introduced normal-runtime workflow-metadata writer.
- [ ] #4 No new documentation inventory/index/manifest was added to track the audit.
- [ ] #5 No behavior or storage model is changed in this Mission beyond guardrails needed to establish the boundary.
- [ ] #6 Any contradiction between landed ADRs and this wave causes a human-stop result rather than an implementation guess.
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 Verification gate ran and passed on the final tree with captured proof rather than an unverified claim
- [ ] #2 Lint and static analysis report clean on every changed file
- [ ] #3 No focused or unannotated skipped tests were introduced (no .only and no bare .skip)
- [ ] #4 Final checkpoint Goal Check table cites real evidence using file:line references and test names
- [ ] #5 Docs updated to reflect any workflow or user-facing behavior change
- [ ] #6 Bug-labeled missions include a red-to-green reproduction test that fails before the fix and passes after
<!-- DOD:END -->
