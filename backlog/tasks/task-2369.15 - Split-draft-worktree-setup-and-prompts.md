---
id: TASK-2369.15
title: Split draft.ts — extract worktree setup, prompt building, and conflict classification
status: backlog
assignee: []
created_date: '2026-08-12'
labels: [ai_sdlc]
dependencies: []
---

## Description

`src/adapters/cli/commands/draft.ts` (1293 lines) mixes command orchestration, worktree/branch setup, graphify bootstrap, prompt building, conflict classification, and stats recording. Split by concern.

Extract to `src/adapters/cli/commands/draft-setup.ts`: `ensureMissionBranch()`, `ensureMissionBaseBranchRecorded()`, `ensureWorktree()`, `ensureGraphifyWorkspace()`, `ensureGraphifyIgnore()`, `ensureMissionFile()`, `ensureDraftRepoConfigCommitted()`, `ensureRepoExists()`, `bootstrapBacklogTask()`, `resolveDraftTarget()`, `slugifyDraftIntent()`, `syntheticTaskId()`, `SYNTHETIC_SLUG_PREFIX`.

Extract to `src/adapters/cli/commands/draft-prompts.ts`: `buildDraftPrompt()`, `buildRestartPrompt()`, `fallbackDraftCommitMessage()`, `resolveMissionClassificationResolver()`, `validateDraftClassification()`, `normalizeDraftClassification()`, `resolveVerifyCmd()`, `resolveTaskPath()`, `resolveClassificationInstructions()`.

Extract to `src/adapters/cli/commands/draft-conflicts.ts`: `classifyDraftEntries()`, `isUnmergedStatus()`, `isDeletedStatus()`, `isMissionTaskPath()`, `isExpectedDraftPath()`, `parseDirtyEntry()`, `resolveMissionSpecificDraftConflicts()`, `enforceDraftCommitSafety()`.

Extract to `src/adapters/cli/commands/draft-stats.ts`: `recordDraftStats()`, `recordDraftImplementer()`, `restartDraftAgent()`, `createDraftWorkflowAdapter()`.

## Files

- `src/adapters/cli/commands/draft.ts` — remove extracted functions, re-export
- `src/adapters/cli/commands/draft-setup.ts` — new file
- `src/adapters/cli/commands/draft-prompts.ts` — new file
- `src/adapters/cli/commands/draft-conflicts.ts` — new file
- `src/adapters/cli/commands/draft-stats.ts` — new file

## Acceptance Criteria

- draft.ts drops below 300 lines (thin command entry + re-exports)
- `./scripts/verify-local.sh static-analysis` passes
