---
id: TASK-2369.16
title: Split setup-review.ts — extract token/auth setup and repo bootstrap
status: backlog
assignee: []
created_date: '2026-08-12'
labels: [ai_sdlc]
dependencies: []
---

## Description

`src/adapters/review/setup-review.ts` (1216 lines) mixes wizard prompting, token creation, repo setup, collaborator management, remote config, and workflow config writing. Split auth/token and repo/bootstrap concerns.

Extract to `src/adapters/review/setup-auth.ts`: `REVIEW_TOKEN_SCOPES`, `PASSWORD_PROMPT_OPTIONS`, `buildTokenName()`, `passwordPrompt()`, `tokenFilePath()`, `ensureTokenDir()`, `resolveBootstrapForgejoHome()`, `writeToken()`, `isAuthFailure()`, `createToken()`, `createTokenWithRetries()`, `tokenCreateViaOwnerToken()`, `apiRequest()`.

Extract to `src/adapters/review/setup-repo.ts`: `ensureRepo()`, `ensureRepoCollaborator()`, `ensureRepoCollaborators()`, `ensureForgejoUser()`, `ensureReviewRemote()`, `bootstrapReviewSurface()`, `suggestedForgejoUsers()`.

Extract to `src/adapters/review/setup-config.ts`: `buildReviewAdapterConfig()`, `buildWorkflowConfig()`, `writeWorkflowConfig()`, `standardLayoutDescription()`, `evaluateReviewSetup()`, `readConfiguredReviewRemote()`, `runVerifyEnv()`, `buildNonInteractiveAnswers()`.

Extract to `src/adapters/review/setup-wizard.ts`: `parseRepoSlug()`, `unique()`, `normalizeBaseUrl()`, `parseYesNo()`, `defaultProductName()`, `defaultRepoSlug()`, `detectPrimaryBranchDefault()`, `promptLine()`, `collectSetupAnswers()`, `collectWizardAnswers()`, `setupReview()`, `setupWizard()`.

## Files

- `src/adapters/review/setup-review.ts` — remove extracted functions, re-export
- `src/adapters/review/setup-auth.ts` — new file
- `src/adapters/review/setup-repo.ts` — new file
- `src/adapters/review/setup-config.ts` — new file
- `src/adapters/review/setup-wizard.ts` — new file

## Acceptance Criteria

- setup-review.ts drops below 200 lines (thin entry + re-exports)
- `./scripts/verify-local.sh static-analysis` passes
