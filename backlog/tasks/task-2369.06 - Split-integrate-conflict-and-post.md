---
id: TASK-2369.06
title: Split integrate.ts — extract conflict resolution and post-integration
status: active
assignee: [vibe]
created_date: '2026-08-12'
labels: [ai_sdlc]
dependencies: []
---

## Description

Extract conflict resolution, worktree path rewrite, squash/stash helpers, stats recording, and cleanup from `integrate.ts` into `src/adapters/cli/commands/integrate-conflict.ts` and `src/adapters/cli/commands/integrate-post.ts`.

Extract to `integrate-conflict.ts`: `resolveConflictsForMission()`, `buildConflictResolutionPrompt()`, `rewriteWorktreePaths()`, `stashMainCheckoutIfNeeded()`, `restoreMainCheckoutStash()`, `getUnresolvedIndexConflicts()`, `areAllBacklogOnlyConflicts()`, `parseStashPopCollisionFiles()`, `reportStashPopFailure()`, `findExistingSquashCommit()`, `prepareNoisePatchForSquash()`, `restoreNoisePatchAfterSquash()`, `maybeUpdateGraphifyOnPrimary()`.

Extract to `integrate-post.ts`: `recordPostIntegrationStats()`, `recordPostIntegrationStatsOrAbort()`, `persistLandedIntegrationOrAbort()`, `runPostIntegrateHookOrAbort()`, `cleanupMissionWorktree()`, `recordStageStats()`, `accumulateStageStats()`, `defaultPrFixRounds()`, `recordIntegrationStats()`, `recordActiveStats()`, `recordReviewStats()`, `telemetryToStatsFields()`, `isIntegrationStatsDataset()`, `formatRecordedStatsRow()`, `shellQuote()`, `resolveForgejoUserForIntegration()`, `SYNC_MERGED_DIAGNOSTICS`, `printDiagnosticTable()`, `reportSyncMergedFailure()`, `isNoMergeToAbortResult()`.

Also extract `classifyHookFailure()` and `handleHookFailureAutoBounce()` — see task-2369.15 for dedup with `rebase-workflow.ts`.

## Files

- `src/adapters/cli/commands/integrate.ts` — remove extracted functions, re-export
- `src/adapters/cli/commands/integrate-conflict.ts` — new file
- `src/adapters/cli/commands/integrate-post.ts` — new file

## Acceptance Criteria

- integrate.ts drops below 1000 lines total (after .04 + .05 + .06)
- `./scripts/verify-local.sh static-analysis` passes
