---
id: TASK-2369.07
title: Split review-commands.ts — extract CLI flag parsing and ReviewWorkflowAdapter
status: review
assignee: [codex]
created_date: '2026-08-12'
labels: [ai_sdlc]
dependencies: []
---

## Description

`src/adapters/review/review-commands.ts` (1941 lines) mixes CLI flag parsing, static review evidence helpers, review command implementations, and the `ReviewWorkflowAdapter` class. Extract CLI concerns and the adapter class.

Extract to `src/adapters/review/review-cli-flags.ts`: `REVIEW_FLAGS`, `REVIEW_VALUE_FLAGS`, `unknownReviewFlags()`, `flagValue()`, `repeatedFlagValues()`, `readTextFlag()`, `unwrapHandoffModule()`, `getHandoff()`, `DEFAULT_MAX_ATTEMPTS`.

Extract to `src/adapters/review/review-workflow-adapter.ts`: `ReviewWorkflowAdapter` class, `createReviewWorkflowAdapter()`.

## Files

- `src/adapters/review/review-commands.ts` — remove extracted code, re-export
- `src/adapters/review/review-cli-flags.ts` — new file
- `src/adapters/review/review-workflow-adapter.ts` — new file

## Acceptance Criteria

- review-commands.ts drops by ~400 lines
- `./scripts/verify-local.sh static-analysis` passes
