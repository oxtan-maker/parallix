---
id: TASK-2369.09
title: Split review-loop.ts — extract gate handling and agent fallback
status: refined
assignee: [codex]
created_date: '2026-08-12'
labels: [ai_sdlc]
dependencies: []
---

## Description

`src/adapters/review/review-loop.ts` (1823 lines) mixes review loop orchestration, pre-review gate handling, gate failure auto-bounce, agent fallback, and stage launch tracking. Extract gate and fallback concerns.

Extract to `src/adapters/review/review-gate-handling.ts`: `classifyGateFailure()`, `PreReviewGateResult`, `runPreReviewGate()`, `handleGateFailureAutoBounce()`, `NO_GATE_NOTICE_ALIAS`, `strictlyLaterIso()`, `DEFAULT_MAX_ATTEMPTS`, `CONTINUE_SKIP_CHECK_TIMEOUT_MS`.

Extract to `src/adapters/review/review-agent-fallback.ts`: `applyAgentFallback()`, `persistNormalizedPhaseRepair()`, `selectPreparedReviewer()`, `stageWindowKey()`, `stageLaunchSinceMs()`, `stageLaunchFingerprint()`, `markStageLaunchRecorded()`, `recordStageStatsSafe()`, `getStats()`, `getHandoff()`, `maybeUpdateGraphifyBeforeReview()`.

## Files

- `src/adapters/review/review-loop.ts` — remove extracted functions, re-export
- `src/adapters/review/review-gate-handling.ts` — new file
- `src/adapters/review/review-agent-fallback.ts` — new file

## Acceptance Criteria

- review-loop.ts drops below 1100 lines
- `./scripts/verify-local.sh static-analysis` passes
