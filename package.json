{
  "name": "@magnus/parallix",
  "version": "1.0.0",
  "description": "parallix — AI mission workflow toolkit with a px CLI, distributed as a local npm tarball",
  "license": "AGPL-3.0-or-later",
  "private": false,
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "bin": {
    "px": "px.js"
  },
  "files": [
    "index.js",
    "px.js",
    "LICENSE",
    "README.md",
    "CHANGELOG.md",
    "config/",
    "data/",
    "docs/",
    "examples/",
    "lib/",
    "prompts/",
    "templates/",
    "tools/setup-forgejo-docker.sh"
  ],
  "scripts": {
    "test": "FORCE_COLOR=0 node --test test/*.test.js",
    "test:coverage": "FORCE_COLOR=0 node lib/commands/coverage-gate.js --lcov"
  },
  "devDependencies": {
    "sonarqube-scanner": "^4.0.1"
  }
}
