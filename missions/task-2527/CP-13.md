# CP-13: Final verification

## Summary

Registered the focused test in the CI-safe integration tier because it creates
only temporary Git worktrees; it neither contacts SonarQube nor depends on a
developer service. The focused suite, test-tier registry checks, and static
analysis now pass.

`./scripts/verify-local.sh all` remains blocked by one inherited failure in
`test/task-2526-web-summary-repro.test.ts`: `web mission summary renders folded
YAML description text`. This branch does not modify its parser or test, and
the failure also occurs on the current mainline baseline documented by the
preceding checkpoints. Repairing it here would violate the mission scope.

## Goal Check

| Criterion | Evidence | Status |
|---|---|---|
| `npm run sonar:setup` creates or reuses one shared local scanner token | `scripts/sonar-local.ts`; test `local SonarQube setup stores one shared token and scanner reuses it` in `test/task-2527-local-sonar.test.ts` | PASS |
| A clean-shell `npm run sonar` submits `parallix` to `http://127.0.0.1:9000` without a prompt | `npm run sonar:up`, `npm run sonar`; SonarQube CE task `23315ebd-5b04-41ad-aef9-1b459667a718` (`componentKey=parallix`, `status=SUCCESS`) | PASS |
| Token is only in the shared ignored Forgejo-resolved home with owner-only permissions and sibling reuse | `src/adapters/forgejo/forgejo-auth.ts`; test `local SonarQube token path discovers the shared sibling-worktree home`; `scripts/sonar-local.ts` | PASS |
| Loopback URL, project inputs, coverage path, quality-gate wait, and Compose loopback publication remain unchanged | `sonar-project.properties`, `infra/sonarqube/compose.yml` | PASS |
| Focused coverage proves bootstrap and scanner invocation without Docker or real SonarQube | `test/task-2527-local-sonar.test.ts`; `npx tsx --test test/task-2527-local-sonar.test.ts test/default-test-suite.test.ts test/test-categories.test.ts` | PASS |
| Static-analysis gate passes | `./scripts/verify-local.sh static-analysis` (exit `0`) | PASS |
| Full verifier passes | `./scripts/verify-local.sh all` (exit `1`; `tests 2640`, `pass 2639`, `fail 1`): inherited test `web mission summary renders folded YAML description text` in `test/task-2526-web-summary-repro.test.ts` | BLOCKED (external task-2526 regression) |

Next action: integrate task-2526's parser repair, then rerun `./scripts/verify-local.sh all`; task-2527 has no in-scope change that can clear that inherited failure.
