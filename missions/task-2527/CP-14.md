# CP-14: Retain only unresolved shared rebase conflicts

## Summary

Corrected the review-handoff recovery path: a conflict-resolution agent launch
is historical context, not evidence that a shared conflict remains. The adapter
now reports a shared-file conflict only when its final classification still
contains shared files. This prevents a later rebase postcondition failure from
being mislabeled as a manual conflict and then as infrastructure.

## Goal Check

| Criterion | Evidence | Status |
|---|---|---|
| `npm run sonar:setup` creates or reuses one shared local scanner token | `scripts/sonar-local.ts`; test `local SonarQube setup stores one shared token and scanner reuses it` in `test/task-2527-local-sonar.test.ts` | PASS |
| A clean-shell `npm run sonar` submits `parallix` to `http://127.0.0.1:9000` without a prompt | `npm run sonar:up`, `npm run sonar`; `missions/task-2527/CP-13.md` | PASS |
| Token is only in the shared ignored Forgejo-resolved home with owner-only permissions and sibling reuse | `src/adapters/forgejo/forgejo-auth.ts`; test `local SonarQube token path discovers the shared sibling-worktree home` | PASS |
| Loopback URL, project inputs, coverage path, quality-gate wait, and Compose loopback publication remain unchanged | `sonar-project.properties`, `infra/sonarqube/compose.yml` | PASS |
| Focused coverage proves bootstrap and scanner invocation without Docker or real SonarQube | `test/task-2527-local-sonar.test.ts`; `npx tsx --test test/task-2527-local-sonar.test.ts test/default-test-suite.test.ts test/test-categories.test.ts` | PASS |
| Static-analysis gate passes | `./scripts/verify-local.sh static-analysis` | PASS |
| Full verifier passes | `./scripts/verify-local.sh all`; inherited task-2526 regression remains recorded in `missions/task-2527/CP-13.md` | BLOCKED |
| A completed conflict-resolution attempt is not reported as an unresolved shared-file conflict | test `rebaseBeforeReviewRound does not report a resolved conflict as shared-file conflict` in `test/review.test.ts`; `npm test -- test/review.test.ts` | PASS |

Next action: submit this repair with `px review task-2527 --start` and let the rebase/handoff run against the current local `main`.
