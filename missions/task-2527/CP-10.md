# CP-10: Review round 3 resolution — task-2526 regression test restored

## Summary

Round 3 raised two findings, both rooted in commit `f16e100c8`
("review(task-2527): drop orphaned task-2526 web-summary test"), which deleted
`test/task-2526-web-summary-repro.test.ts` — a file that exists on the mission
merge-base `main` — to clear the inherited unit-suite failure that round 2
forbade this mission from repairing.

Both findings are fixed by restoring that file to its `main` content:

- **F1 (deleting the test suppresses a real defect instead of stopping):**
  `test/task-2526-web-summary-repro.test.ts` is restored from `main`. Deleting
  another mission's regression test is the same class of scope violation as the
  cherry-pick that round 2 rejected, and it is worse: the cherry-pick at least
  left the block-scalar parsing defect fixed, while the deletion left the defect
  on `main` with no test asserting it, and invalidated the recorded acceptance
  evidence of the completed `task-2526` (`backlog/completed/task-2526 -
  Web-does-not-display-a-good-summary-of-a-mission.md`, criterion `#6`). The
  mission stops under its Stop Rule instead, and `task-2526` integrates its own
  fix together with its own test.
- **F2 (final checkpoint does not describe the reviewed tree):** resolved by the
  same restoration. `missions/task-2527/CP-9.md` claims the mission diff outside
  `missions/` and `backlog/` is exactly one file, and that claim is true again of
  this tree. This checkpoint additionally records the deletion and its reversal so
  the checkpoint chain, not just the diff, states what happened.

No SonarQube-facing file changed this round: `package.json`,
`sonar-project.properties`, and `infra/sonarqube/compose.yml` remain identical to
`main`, and `src/adapters/backlog/task-file-io.ts` remains identical to `main`.

## Evidence

- Mission diff outside `missions/` and `backlog/` is one added file again:

```
$ git diff main --stat -- . ':(exclude)missions' ':(exclude)backlog'
 test/task-2527-local-sonar-no-auth.test.ts | 19 +++++++++++++++++++
 1 file changed, 19 insertions(+)
```

- `./scripts/verify-local.sh static-analysis` — exit `0`,
  `=== Static Analysis Gate: ALL STAGES PASSED ===`.
- `./scripts/verify-local.sh all` — exit `1`: `tests 2641`, `pass 2640`,
  `fail 1`. The single failure is again
  `test/task-2526-web-summary-repro.test.ts`, test `web mission summary renders
  folded YAML description text`. This is the inherited `main` failure documented
  in `missions/task-2527/CP-9.md`, restored deliberately: the mission has no
  in-scope way to clear it, and hiding it by deleting the test is what round 3
  rejected.
- `npx tsx --test test/task-2527-local-sonar-no-auth.test.ts` —
  `✔ local SonarQube scan submits without authentication configuration`
  (`pass 1`, `fail 0`).

The SonarQube goal remains halted under Stop Rule 1, unchanged since
`missions/task-2527/CP-5.md`: `sonarqube:25.6.0.109173-community` keeps
`sonar.forceAuthentication` locked to `true` even with
`SONAR_FORCEAUTHENTICATION=false` present in the container, and anonymous
`GET /api/plugins/installed` returns `403`.

## Goal Check

| Criterion | Evidence | Status |
|---|---|---|
| Round 3 finding F1 resolved: no unrelated test deletion in the mission diff | `test/task-2526-web-summary-repro.test.ts` restored from `main`; `git diff main --stat` reports only `test/task-2527-local-sonar-no-auth.test.ts` | PASS |
| Round 3 finding F2 resolved: the final checkpoint describes the reviewed tree | `missions/task-2527/CP-9.md` one-file diff claim holds again; `missions/task-2527/CP-10.md` records the deletion and its reversal | PASS |
| Clean-shell `npm run sonar` submits `parallix` to `http://127.0.0.1:9000` without a 401, missing-token error, or prompt | `npm run sonar:up`, `npm run sonar`; anonymous `GET /api/plugins/installed` returns `403` on `sonarqube:25.6.0.109173-community` (`missions/task-2527/CP-5.md`) | BLOCKED (Stop Rule 1) |
| Local scanner configuration introduces no token, login, password, secret file, remote host URL, or credential-management command | `package.json`, `sonar-project.properties`, `infra/sonarqube/compose.yml`, `test/task-2527-local-sonar-no-auth.test.ts` | PASS |
| `sonar-project.properties` keeps `sonar.host.url=http://127.0.0.1:9000` and `infra/sonarqube/compose.yml` keeps `127.0.0.1:9000:9000` | `sonar-project.properties`, `infra/sonarqube/compose.yml` (both identical to `main`), `test/task-2527-local-sonar-no-auth.test.ts` | PASS |
| Focused test `local SonarQube scan submits without authentication configuration` asserts the no-token invocation and loopback endpoint without Docker or a real service | `test/task-2527-local-sonar-no-auth.test.ts`, `npx tsx --test test/task-2527-local-sonar-no-auth.test.ts` (`pass 1`, `fail 0`) | PASS |
| `./scripts/verify-local.sh static-analysis` exits zero on the final tree | `./scripts/verify-local.sh static-analysis` (exit `0`) | PASS |
| `./scripts/verify-local.sh all` exits zero on the final tree | `./scripts/verify-local.sh all` (exit `1`, `tests 2641`, `pass 2640`, `fail 1`): inherited `test/task-2526-web-summary-repro.test.ts` failure, fix pending on `review/mission/task-2526` | BLOCKED (external dependency; rounds 2 and 3 forbid both repairing and deleting it here) |

Next action: round 3's F1 and F2 are fixed by restoring `test/task-2526-web-summary-repro.test.ts`, and the mission diff is back to the single focused test, so task-2527 stops here and waits on two external decisions — `review/mission/task-2526` integrating its parser patch so `./scripts/verify-local.sh all` can exit zero, and product direction on the SonarQube goal under Stop Rule 1 (change the image or version, accept one local token, or close task-2527 as unachievable without credentials).
