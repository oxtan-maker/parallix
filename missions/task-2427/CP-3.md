# CP 3 — Full proof + gates

## Summary of work done

Proved the complete mission matrix against the committed tree, then ran both mission-declared gates.

**Draft characterization suites (SC5/SC7)** — all green with unchanged assertions:
`node --import tsx --experimental-test-module-mocks --test test/draft-command-use-case.test.ts test/draft-command.test.ts test/draft.test.ts test/draft_preflight_modern.test.ts test/draft-extraction.test.ts test/task-2406-draft-current-work.test.ts` → 91/91 pass, 0 fail.

**Board suites (SC1–SC4, SC6, SC8)** — all green:
`node --import tsx --experimental-test-module-mocks --test test/board-controller.test.ts test/task-2427-board-draft.test.ts test/task-2426-repro.test.ts test/command-dispatch-convergence.test.ts test/tui-action-bar.test.ts test/tui-command-flow.test.ts test/production-composition-capabilities.test.ts test/task-2387-board-current-work.test.ts` → 61/61 pass, 0 fail.

**Gates** — `./scripts/verify-local.sh all` → exit 0 (full default suite 2184/2184 pass, 0 fail; suite budget 23s < 180s, no per-test budget exceedances) and `./scripts/verify-local.sh static-analysis` → exit 0 (ESLint clean, tsc typecheck clean, test-hygiene clean, test typecheck clean). Note: two earlier `all` runs exited 1 with 10–13 `[unit-test-budget:exceeded]` warnings in pre-existing heavy git/worktree test files (e.g. `task-2438`, `performHandoff handles gatekeeper pushback`) while all 2184 tests passed; the runner fails the suite on any single per-test budget exceedance (1000ms hard limit in `test/lib/unit-test-budget-reporter.ts`). Machine load at the time (load average ~12–21 on 16 cores, repo on ecryptfs) pushed those pre-existing tests over the line; the tests run in ~200ms in isolation. After the load dropped, the gate passed with zero exceedances. No mission change touches any of the over-budget tests; the mission's new/updated tests are mocked and fast.

**Review repair (F1):** The production board adapter now injects the throwing boundary into `ensureWorktree` and replaces direct agent-config exits with `readAgentConfig`; the post-process restart also receives that reader. `test/task-2426-repro.test.ts` — `"production draft adapter maps worktree creation abort to typed failure without exiting"` exercises the real adapter through its `gitFn` seam and proves the controller returns typed `failure` instead of terminating the host.

**Guardrail verification (SC7):** `git diff --name-only 9292137ae..HEAD -- src/adapters/cli/commands/draft.ts src/application/execute-mission-service.ts` is empty (base `9292137ae` = parent of the first mission commit) — the CLI `px draft` entry and the `active:execute` service are byte-unchanged. `grep -n "child_process\|spawn(" src/application/controller/board-controller.ts src/application/draft-command-use-case.ts src/composition/production-capabilities.ts` has no matches — the board draft path spawns no `px draft` subprocess and parses no command string.

**Bookkeeping:** committed the one-line `src/application/consumer-domain-requirements.ts` line-reference refresh (`line: 274 → 278`, anchor `function projectMissionCard`) left uncommitted at the end of CP 2, verified against `src/application/projections/mission-board.ts` where the function now sits at line 278. Ran `graphify update .` after the code changes (no topology changes detected; graph left consistent).

**Exact typed fields the `draft:create` board request accepts** (for the SC2 row below): the request is a `BoardCommandRequest` with `operationId: string`, `kind: 'draft:create'`, `missionId: string` (the mission slug — the only mission value; the narrow typed entry `DraftCommandUseCase.executeForSlug(slug)` receives exactly this), `capabilities: ReadonlySet<Capability>`, plus the shared optional envelope fields `missionStatusAtRequest?: string`, `cancellation?: BoardCancellation`, `detached?: boolean`. `payload` is **absent**: the `BoardCommandPayload` union has exactly three members (`mission:intake`, `checkpoint:record`, `handoff:record`) and no `draft:create` member, so there is no field for an argv `string[]`, options/Record bag, filesystem/worktree path, environment value, or agent-launch option. The shared `agent?: string | null` field keeps its `active:execute` meaning only and is not forwarded to the draft workflow.

## Goal Check

| Criterion | Evidence | Status |
|---|---|---|
| SC1 (registry): `draft:create` integrated; other four kinds keep their reasons | `test/board-controller.test.ts` — `"INTEGRATED_CAPABILITIES contains active:execute and the Mission commands"`, `"UNAVAILABLE_CAPABILITIES has reasons for all four unextracted commands"`, `"isIntegratedCapability covers the Mission commands extracted so far"`, `"controller rejects draft:create with the typed unavailable capability when unwired"`; registry in `src/application/controller/board-command.ts` | PASS |
| SC2 (typed boundary, exact fields): request accepts `operationId`, `kind: 'draft:create'`, `missionId` (slug), `capabilities`, shared optional envelope fields only; no payload member (no argv/options/path/env/agent field) | `test/task-2427-board-draft.test.ts` — `"draft request carries only the slug: extra args/options/env/path/agent values never reach the workflow"` (hostile request with smuggled `args`/`options`/`env`/`worktreePath`/`agent` values; asserts the workflow received `args: ['task-2427']`, `options: {}`, and none of the smuggled values), `"executeForSlug is the typed entry: it forwards only the slug and empty options"`; `BoardCommandPayload` union in `src/application/controller/board-command.ts` (three members, no `draft:create`) | PASS |
| SC3 (availability from authoritative state): `draft` enabled only for open ∧ `status === 'backlog'`; `canExecute` false unwired / true wired; non-draftable state rejected with zero workflow calls | `test/task-2427-board-draft.test.ts` — `"canExecute(draft:create) is false unwired and true wired"`, `"non-draftable lifecycle states reject before the draft workflow port is called"`, `"unwired controller returns the typed unavailable capability without touching mission authority"`; `test/tui-action-bar.test.ts` — `"canDispatchAction gates draft:create on wiring and pre-draft eligibility"`; `availableBoardCommands` in `src/application/projections/mission-board.ts` | PASS |
| SC4 (failures never project as success): aborts map to `failure`/`rejected`, never `completed`; blocked publication preserved | `test/task-2427-board-draft.test.ts` — `"preflight abort (unresolvable task) returns failure and publishes nothing"`, `"existing-worktree abort after launch publishes blocked and returns failure"`, `"a thrown workflow abort (exit function throwing) maps to failure with blocked publication"`; `test/task-2426-repro.test.ts` — `"production draft adapter maps worktree creation abort to typed failure without exiting"` | PASS |
| SC5 (workflow + publication preserved): 9-step sequence unchanged; CLI characterization suites green without weakened assertions | `test/draft-command-use-case.test.ts`, `test/draft-command.test.ts`, `test/draft.test.ts`, `test/draft_preflight_modern.test.ts`, `test/draft-extraction.test.ts`, `test/task-2406-draft-current-work.test.ts` — 91/91 pass via `node --import tsx --experimental-test-module-mocks --test <those six files>` | PASS |
| SC6 (trusted composition): wired graph supplies the trusted adapter; its legacy exit helpers use the throwing boundary; read-only graph typed unavailable | `test/task-2426-repro.test.ts` — `"wired production controller drafts through the injected workflow port"`, `"production composition builds the trusted draft adapter by default (no override needed)"`, `"production draft adapter maps worktree creation abort to typed failure without exiting"`, `"read-only production controller does not advertise Mission commands"`; `createBoardDraftService` in `src/composition/production-capabilities.ts` | PASS |
| SC7 (CLI unchanged, no smuggling path): `px draft` argv/`--agent` parsing stays in the CLI adapter; no board subprocess or server-side command-string parse | `git diff --name-only 9292137ae..HEAD -- src/adapters/cli/commands/draft.ts` → empty (restricted area untouched vs mission base); no `child_process`/`spawn(` in `src/application/controller/board-controller.ts`, `src/application/draft-command-use-case.ts`, or `src/composition/production-capabilities.ts`; CLI characterization suites green (in the 91/91 run above); `./scripts/verify-local.sh all` | PASS |
| SC8 (no regressions in the matrix): board suites green, other kinds' outcomes unchanged | `test/board-controller.test.ts`, `test/task-2426-repro.test.ts`, `test/command-dispatch-convergence.test.ts`, `test/tui-action-bar.test.ts`, `test/tui-command-flow.test.ts`, `test/production-composition-capabilities.test.ts`, `test/task-2387-board-current-work.test.ts`, `test/task-2427-board-draft.test.ts` — 61/61 pass via `node --import tsx --experimental-test-module-mocks --test <those eight files>`; full suite 2184/2184 in `./scripts/verify-local.sh all` | PASS |
| Gate: `./scripts/verify-local.sh all` passes | `./scripts/verify-local.sh all` — exit 0, full default suite `tests 2184 / pass 2184 / fail 0`, `[unit-test-budget] elapsed=23260ms` < 180s budget, no `[unit-test-budget:exceeded]` lines | PASS |
| Gate: `./scripts/verify-local.sh static-analysis` passes | `./scripts/verify-local.sh static-analysis` — exit 0, "PASS: ESLint clean", "PASS: tsc typecheck clean", "PASS: test-hygiene clean", "PASS: test typecheck clean" | PASS |
| Web-boundary motivation: slug-only typed boundary is what the ADR 0054 typed mutation endpoint will dispatch through | `ADR 0054`; `BoardCommandPayload` in `src/application/controller/board-command.ts` (no `draft:create` member) | PASS |

Next action: hand off — every declared checkpoint (CP-1, CP-2, CP-3) is committed and both mission gates pass on the committed tree; nothing left in scope.
