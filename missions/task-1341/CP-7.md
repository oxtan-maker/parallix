# CP-7

The optional-task-file flow is now wired through draft, preflight, gatekeeper, integrate, stats, and the README, and the remaining regressions exposed by the full suite and smoke runs were closed. Synthetic drafts keep `labels: [unknown]`, downstream reports count `unknown` explicitly, adapter-backed task storage replaces the remaining hardcoded task-path assumptions, and the synthetic draft prompt now tells the agent to preserve `unknown` instead of forcing `ai_sdlc` or `user_value`.

## Goal Check
| Goal Check description | Evidence | Status |
| --- | --- | --- |
| Free-text draft synthesizes a slug and creates an unknown-classification task instead of requiring a pre-existing backlog file. | lib/commands/draft.js:107; lib/commands/draft.js:583; lib/commands/draft.js:593; test `runDraftCommand accepts free-text intent and synthesizes a task slug`; test `bootstrapBacklogTask creates a synthetic unknown-classification task when requested`; missions/task-1341/SMOKE.md:3 | PASS |
| Directory-mode draft creates a synthetic task with a deterministic synthetic ID and `labels: [unknown]`. The round-3 `adhoc-`/`ADHOC-` reservation supersedes the criterion's literal `TASK-{basename-hash}` wording, matching the locked Risk-section mitigation that synthetic tasks should carry a reserved prefix. | lib/commands/draft.js:18; lib/commands/draft.js:32; lib/commands/draft.js:585; lib/commands/draft.js:588; lib/commands/draft.js:593; missions/task-1341/SMOKE.md:14 | PASS |
| Synthetic draft prompts preserve `unknown` classification for synthetic tasks instead of instructing the agent to overwrite it. | prompts/draft.md:12; lib/commands/draft.js:659; test `buildDraftPrompt preserves unknown classification instructions for synthetic tasks`; missions/task-1341/SMOKE.md:10 | PASS |
| Stats accepts `unknown`, reports it in weekly summaries, and historical backfill still falls back to git history when `unknown` is only a sentinel. | lib/commands/stats.js:35; lib/commands/stats.js:556; lib/commands/stats.js:1179; lib/commands/stats.js:1220; lib/commands/stats-backfill.js:218; test `resolveMissionClassification returns unknown when no task file exists`; test `upsertStatsRow accepts unknown classification rows and weekly report counts them`; test `collectHistoricalStatsBackfill falls back to git history for date and human implementer`; missions/task-1341/SMOKE.md:24 | PASS |
| Mission-start, gatekeeper, and integrate no longer hard-fail on missing task files when mission artifacts exist or synthetic metadata is available. | lib/commands/mission-start.js:153; lib/tools/gatekeeper.js:44; lib/commands/integrate.js:1011; test `missionStart passes when the task file is missing and classification falls back to unknown`; test `checkMandatoryFiles accepts missing backlog task file when mission artifacts exist`; test `printIntegrationPreflight tolerates a missing task file and reports unknown classification`; test `recordPostIntegrationStats records an unknown classification row for a missing-task mission` | PASS |
| Remaining task-path and setup wording assumptions were normalized around adapter-backed markdown task storage instead of hardcoded Backlog.md-only phrasing. | lib/core/product-config.js:368; lib/tools/backlog.js:12; lib/tools/backlog.js:123; lib/tools/backlog.js:491; lib/tools/setup-review.js:91; lib/tools/setup-review.js:614; test `collectWizardAnswers uses default layout answers and bootstrap defaults` | PASS |
| The README quick start now leads with `px draft` free text and `px draft .`, and the first 80 lines no longer require task-file creation. | README.md:11; README.md:17; README.md:64; README.md:77; README.md:83; missions/task-1341/CP-6.md:5 | PASS |
| All declared gates passed: full suite green, free-text draft smoke green, directory-mode draft smoke green, and stats report shows an `unknown` bucket. | missions/task-1341/SMOKE.md:3; missions/task-1341/SMOKE.md:14; missions/task-1341/SMOKE.md:24; missions/task-1341/SMOKE.md:31 | PASS |

## Parked Follow-up

Adhoc mission slugs are now reserved correctly, but several numeric `task-\d+` extractors outside this mission's editable scope still do not recognize `adhoc-*` slugs. In practice that means Forgejo PR slug extraction and stats backfill keep their pre-existing non-numeric-slug limitation for adhoc missions until a follow-up can widen those matchers in the restricted review/Forgejo paths and related backfill helpers.

Graphify follow-up was attempted per repo instructions, but the local `graphify` CLI is not installed in this environment: `missions/task-1341/SMOKE.md:39`.

Next action: stage the checkpoint set and code changes together, commit them on the mission branch, and hand off with the smoke evidence and green suite attached.
