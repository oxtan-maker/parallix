# CP-10 — Round-7 resolution (revert `5eaa6fac7`; CP-8's state stands)

## Work done

Round 7 accepted CP-8 in full and asked for one narrow reversal: `5eaa6fac7`
(commit message: `fixes`) landed after CP-8 and undid its central point in three
places at once. All three are reverted here. No new work was added.

**F1 — review assembly is the pre-split prompt again.** `5eaa6fac7` moved
`- Review as an independent senior engineer. Approve only if the mission is
satisfied, verification is credible for the risk level, and the diff is safe to
integrate.` out of the `Minimum loop contract:` list in `prompts/review-core.md`
and into `prompts/review.md`. Because the opinion half is appended after the
core, that bullet re-emitted as a trailing orphan below the `You MAY` block —
the round-3 F1 defect, regressed for the review stage. The line is restored to
its parent position as the fourth item of `Minimum loop contract:`, directly
after the `px status {{slug}}` bullet, and `prompts/review.md` is empty again
(0 bytes).

**F2 — the ordered checks are restored.** The three relaxations in
`test/prompt-split.test.ts` are reverted:

- `assertLaunchKeepsEveryLine` loses the `expectedUnordered` parameter and its
  `multisetEqual` branch, so every stage — `review` included — is back under
  `isOrderedSubsequence(nonPlaceholderSeq(parent), nonPlaceholderSeq(launch))`.
- `task-2465-assembly-*` is named `rebuilds the parent-commit prompt` again and
  the `if (stage === 'review')` multiset special case is gone; all five stages
  assert `assert.equal(assembled, parentPrompt(stage))`.
- `normalizeBlankRuns` is restored, so review's blank-line structure is checked
  again.

The 18/18 pass is now evidence of ordered, byte-identical assembly for all five
stages rather than of a permutation-tolerant multiset.

**F3 — the locked contract is back to its drafted text.**
`missions/task-2465/MISSION.md` is byte-identical to the drafted revision:
`git diff --quiet d97f1f931 -- missions/task-2465/MISSION.md` exits 0. Scope
bullet 1 no longer carries `A shipped default-opinion file may be empty when
that stage has no replaceable opinion.`, and Success Criterion 3 reads
`produces the same instruction content as its corresponding pre-split prompt`
with the review carve-out sentence removed. CP-9's claim of authority rested on
that amended text and is superseded by this checkpoint.

**The three-stage design conflict remains parked.** Draft, execute and review
have no contiguous non-core suffix, so their shipped default-opinion halves are
empty. Round 7 states explicitly that this is an operator decision and does not
ask for it to be resolved in code. CP-8's disposition — park it, state it, leave
the contract alone — stands, and this checkpoint does not treat the reverted
Scope sentence as authority for anything.

**F4 — corrected diff-budget measurement.** CP-9 cited
`git diff --numstat 23c5fb759 f23708b33 …` → 97 added lines. Both SHAs are from
round 6 (`23c5fb759` was that round's baseline, `f23708b33` its HEAD), so the
range excluded work present in the reviewed revision. Measured on the reviewed
range instead — `git merge-base main HEAD` = `d59bbf3f0` —
`git diff --numstat d59bbf3f0..HEAD --diff-filter=ACM -- 'src/*.ts' 'src/**/*.ts' 'src/*.js' 'src/**/*.js'`
gives **102 added lines** (`runtime-assets.ts` 52, `product-config.ts` 31,
`review-prompts.ts` 6, `review-loop.ts` 5, `active.ts` 4, `draft-prompts.ts` 4).
The criterion still passes (102 < 150).

**Out-of-scope commit named, as CP-6 named the `task-2284` helper fix.**
`9a3b89fe5 fix(review): demote review-start poll and max-attempts lines to
verbose` changes `src/adapters/review/review-loop.ts` (+5/-2) and is not mission
work. It repairs two failures inherited from `main`, not introduced here:
`test/task-1209-review-loop.test.ts` (`default output hides review plumbing`)
and `test/task-2477-review-presentation.test.ts` (`verbose review start exposes
poll/provider lines that default hides`). `aae3adcba mission/task-2477` landed
those assertions but left the `Focus | Max attempts` and `Poll interval` header
lines in `startReviewLoop` unconditional; reverting the fix and re-running both
files reproduces exactly those two failures on `main`'s code. The fix gates the
two plumbing lines behind the existing `verbose` flag. Its 5 added lines are
included in the 102 above; the mission-attributable count excluding it is 97.

## Gate

`./scripts/verify-local.sh all` — see Goal Check row below for the result.
`npx tsx --test test/prompt-split.test.ts` → 18 tests, 18 pass, 0 fail, with the
ordered and byte-identical assertions restored.

## Goal Check

| Criterion | Evidence | Status |
| --- | --- | --- |
| Ten files under `prompts/` (core + default per stage) | `git ls-files 'prompts/*-core.md' 'prompts/draft.md' 'prompts/execute.md' 'prompts/review.md' 'prompts/act-on-review.md' 'prompts/portfolio.md'` lists the five `prompts/<stage>-core.md` and five `prompts/<stage>.md` files | Pass |
| Preservation test per stage (non-blank multiset equals parent) | `test/prompt-split.test.ts`: `task-2465-0: draft core+default preserves the parent-commit prompt as a non-blank multiset`, `task-2465-1: execute …`, `task-2465-2: review …`, `task-2465-3: act-on-review …`, `task-2465-4: portfolio …` | Pass |
| No-override launch equivalence (5 launch points) | `test/prompt-split.test.ts`: `task-2465-draft launch point preserves every pre-split line (no override)`, and the same for `task-2465-execute`, `task-2465-review`, `task-2465-act-on-review`; portfolio has no wired launch point (CP-1) and is covered by `task-2465-assembly-4: portfolio no-override assembly rebuilds the parent-commit prompt`. All five assembly tests `task-2465-assembly-0..4` now assert `assert.equal(assembled, parentPrompt(stage))` with no per-stage exemption | Pass |
| Override retention of core (cannot drop core) | `test/prompt-split.test.ts`: `task-2465: configured override replaces opinion content but retains a named core instruction` and `task-2465: override cannot drop core even when the repo opinion omits it` | Pass |
| One config key; unknown keys fail via config error path | `test/prompt-split.test.ts`: `task-2465: adapters.prompts.override is accepted; unknown keys beneath it are rejected`; key closed with `additionalProperties: false` in `config/workflow.config.schema.json` | Pass |
| `src/` non-test/non-prompt/non-doc diff < 150 lines | `git diff --numstat d59bbf3f0..HEAD --diff-filter=ACM -- 'src/*.ts' 'src/**/*.ts' 'src/*.js' 'src/**/*.js'` → 102 added lines (97 excluding the out-of-scope `9a3b89fe5` review-loop fix named above); baseline `d59bbf3f0` = `git merge-base main HEAD` | Pass |
| `docs/config.md` documents the key + links defaults | `docs/config.md` Prompts section documents `adapters.prompts.override`, states unconfigured behaviour is unchanged, and links the five shipped default-opinion files | Pass |

Next action: hand back to review with the round-7 findings F1, F2 and F3 fixed by
reverting `5eaa6fac7` and F4 corrected here; the three-stage empty-opinion-half
question stays parked for an operator decision and is not resolved in code.
