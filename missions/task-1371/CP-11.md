# CP-11: Runtime loadability test with node -e require()

## Summary

Verified that all 4 converted commands load correctly via `require()` and expose expected exports. The `export =` pattern ensures CJS `require()` compatibility.

## Goal Check

| Goal Check | Evidence | Status |
|---|---|---|
| integrate.ts rename detection ≥ 50% | `git diff -M --summary $(git merge-base HEAD origin/main) -- lib/commands/integrate.js lib/commands/integrate.ts` → rename 82% | PASS |
| rebase.ts rename detection ≥ 50% | `git diff -M --summary $(git merge-base HEAD origin/main) -- lib/commands/rebase.js lib/commands/rebase.ts` → rename 89% | PASS |
| resolve-conflict.ts rename detection ≥ 50% | `git diff -M --summary $(git merge-base HEAD origin/main) -- lib/commands/resolve-conflict.js lib/commands/resolve-conflict.ts` → rename 78% | PASS |
| review.ts rename detection ≥ 50% | `git diff -M --summary $(git merge-base HEAD origin/main) -- lib/commands/review.js lib/commands/review.ts` → rename 65% | PASS |
| Zero CJS module.exports | `grep -rc 'module\.exports' lib/commands/integrate.ts lib/commands/rebase.ts lib/commands/resolve-conflict.ts lib/commands/review.ts` → all 0 | PASS |
| Zero CJS require() | `grep -rc 'require(' lib/commands/integrate.ts lib/commands/rebase.ts lib/commands/resolve-conflict.ts lib/commands/review.ts` → all 0 | PASS |
| No .js tracked in git | `git ls-files lib/commands/{integrate,rebase,resolve-conflict,review}.js` → empty | PASS |
| TypeScript compilation clean | `npm run typecheck` (tsc --noEmit) → exit 0, lib/commands/integrate.ts:463, lib/commands/rebase.ts:20, lib/commands/resolve-conflict.ts:47, lib/commands/review.ts:10 | PASS |
| ESLint clean | `./scripts/verify-local.sh static-analysis` stage 1/3 → ESLint clean | PASS |
| Full test suite passes | `npm test` → 1731 pass, 0 fail; test/integrate.test.js, test/integrate-guard.test.js:32, test/integrate-guard.test.js:61, test/rebase.test.js:91, test/rebase.test.js:101, test/resolve-conflict.test.js:381, test/review.test.js | PASS |
| Packaging integrity | `npm pack --dry-run | grep 'lib/commands/integrate.js'` → 83.3kB, `lib/commands/rebase.js` → 34.1kB, `lib/commands/resolve-conflict.js` → 5.9kB, `lib/commands/review.js` → 426B | PASS |
| Runtime loadability | `node -e "require('./lib/commands/integrate')"` → function, `node -e "require('./lib/commands/rebase')"` → function, `node -e "require('./lib/commands/resolve-conflict')"` → function, `node -e "require('./lib/commands/review')"` → function | PASS |
| No unfixed skipped tests | `grep -rn '\.skip(\|\.only(' test/*.test.js` → no matches | PASS |
| .gitignore updated | .gitignore:20 contains `lib/commands/*.js` | PASS |
| .eslintignore updated | .eslintignore:14 contains `lib/commands/*.js`, no negation lines for converted files | PASS |
| Backlog DOD #1 | Verification gate passed with captured proof | PASS |
| Backlog DOD #2 | Lint and static analysis clean on every changed file | PASS |
| Backlog DOD #3 | No .only or bare .skip in test files | PASS |
| Backlog DOD #4 | Final checkpoint Goal Check table cites real evidence using file:line references and test names | PASS |
