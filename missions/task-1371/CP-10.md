# CP-10: Run npm run prepublishOnly && npm pack --dry-run

## Summary

Ran `npm run prepublishOnly` (which triggers `build:cjs`) and `npm pack --dry-run`. All 4 compiled `.js` files appear in the package tarball alongside their `.ts` sources.

## Goal Check

| Criterion | Evidence |
|-----------|----------|
| integrate.js in package | `npm pack --dry-run | grep 'lib/commands/integrate.js'` → `npm notice 83.3kB lib/commands/integrate.js` |
| rebase.js in package | `npm pack --dry-run | grep 'lib/commands/rebase.js'` → `npm notice 34.1kB lib/commands/rebase.js` |
| resolve-conflict.js in package | `npm pack --dry-run | grep 'lib/commands/resolve-conflict.js'` → `npm notice 5.9kB lib/commands/resolve-conflict.js` |
| review.js in package | `npm pack --dry-run | grep 'lib/commands/review.js'` → `npm notice 426B lib/commands/review.js` |
| prepublishOnly clean | `npm run prepublishOnly` exits with code 0 |

## Next action
Proceed to CP-11: Runtime loadability test with `node -e require()`.
