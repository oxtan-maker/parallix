# CP-4 — Follow-ups and closure

## Summary

Created the two follow-up backlog tasks for the chosen implementation path, added
the `docs/adr/index.md` entry, added the ADR 0052 cross-references to ADR 0044 and
ADR 0051, and ran all three mission gates on the final tree.

**Follow-up IDs verified free against `main`, not just this worktree.**
`origin/main` is behind this worktree (its highest task id is TASK-2293, while
the local catalog reaches TASK-2314), so a "next free id" derived from either
source alone would have been unsafe. TASK-2315 and TASK-2316 were checked against
`git ls-tree -r --name-only origin/main -- backlog/` *and* against the live main
checkout at `/home/magnus/code/parallix`; both are absent from
`backlog/tasks/`, `backlog/completed/`, and `backlog/archive/` in both places.

- `backlog/tasks/task-2315 - Implement-byte-faithful-task-record-import-and-export-for-the-SQLite-task-catalog.md`
  — import/export, shadow validation, and repair of the two corrupt records.
- `backlog/tasks/task-2316 - Cut-over-task-catalog-authority-to-SQLite-and-retire-Markdown-write-paths.md`
  — the single gated cutover and retirement of the file-authority reconciliation
  machinery.

Each carries an explicit compatibility-check acceptance criterion and an explicit
rollback-path acceptance criterion, both stated as tested behavior rather than
prose.

ADR 0044 and ADR 0051 received a cross-reference line only — no rewrite of their
decisions, no dated-history or "Superseded" narrative, per the repository's
compact-ADR practice.

### Disclosures

**NEL overrun.** The mission's code+test diff is **531 NEL** (532 added, 1
deleted, after ADR 0047 exclusions), against a predicted Medium bucket ceiling of
235. Per the mission's stop rule this is reported rather than worked around, and
the harness was not expanded further after the measurement. The driver is the
proof requirement, not scope creep: criterion 8 requires unknown-extension-key
coverage, and CP-2 found that field-level comparison silently passed while
dropping YAML folded block scalars, so the harness had to model block scalars,
per-item raw spelling, and byte-identity assertion
(`test/helpers/task-2284-catalog-round-trip.ts:17-32`,
`test/task-2284-catalog-round-trip.test.ts:212-216`). Both files are test-tree
only; no production code changed.

**`NOTICES` in the mission diff.** `NOTICES` is regenerated by `npm run build`
(`scripts/release-metadata.js:134-139`) and its version header moved 1.4.37 →
1.4.40 during a CP-2 test run, entering the diff as a one-line build artifact
change. It is not a mission edit and touches no restricted area.

## Goal Check

| Criterion | Evidence | Status |
|---|---|---|
| 1. ADR exists with `Status:`, `Date:`, `Related:`, `## Context`, `## Decision`, `## Consequences`; index contains exactly one bullet naming that path | `docs/adr/0052-task-catalog-authority-and-board-authorship.md:3`, `:4`, `:5`, `:7`, `:224`, `:400`; `docs/adr/index.md:23` | PASS |
| 2. Context inventories all nine behaviors, each with a `file:line` citation or named test; cited paths and lines exist | `docs/adr/0052-task-catalog-authority-and-board-authorship.md:29` (creation), `:46` (ID allocation), `:66` (status transitions), `:79` (integration-branch writes), `:101` (archival/completion), `:118` (Git review), `:130` (portability), `:147` (recovery), `:169` (`backlog.md`); `"pruneStaleBacklogDuplicates removes the stale copy and clears the gate"` | PASS |
| 3. Comparison table with one row per option against the same named criteria columns | `docs/adr/0052-task-catalog-authority-and-board-authorship.md:237` (criteria header), `:239` (Git Markdown), `:240` (SQLite + export), `:241` (append-only events) | PASS |
| 4. Exactly one authority selected; all ten operational concerns each a labelled row | `docs/adr/0052-task-catalog-authority-and-board-authorship.md:255` (selection), `:279` offline, `:284` multi-repo identity, `:292` concurrent writers, `:298` merge/conflict, `:304` backup, `:308` corruption recovery, `:315` downgrade, `:320` import/export, `:327` human inspection, `:331` automation access | PASS |
| 5. Dual-write rejected as steady state; the one permitted compatibility write defines reconciliation rule, telemetry signal, removal gate, and bounded lifetime | `docs/adr/0052-task-catalog-authority-and-board-authorship.md:336`, `:351`, `:355`, `:360`, `:364` | PASS |
| 6. Authoring rule stated client-neutrally, names the application port, forbids direct SQL and direct filesystem task writes, binds every client | `docs/adr/0052-task-catalog-authority-and-board-authorship.md:370-388`, `src/application/controller/board-command.ts:78`, `src/application/controller/board-command.ts:93` | PASS |
| 7. One sentence with a yes/no answer and a reason on Git version control and reviewability | `docs/adr/0052-task-catalog-authority-and-board-authorship.md:392` | PASS |
| 8. Round-trip harness plus `test/task-2284-catalog-round-trip.test.ts` preserving every named frontmatter key, the `SECTION:DESCRIPTION` body, every `AC:` item with checked state and `#n` index, every `DOD:` item, and an unknown extension key | `test/helpers/task-2284-catalog-round-trip.ts:173`, `test/task-2284-catalog-round-trip.test.ts:102-111`, `"round trip preserves every named frontmatter key, including unknown extension keys"`, `"round trip preserves the SECTION:DESCRIPTION body and every AC and DOD item with its checked state and index"` | PASS |
| 9. Test runs against three real files copied to a temp directory (one `backlog/tasks/`, one `backlog/completed/`, this mission's task file) with zero field differences; source files unmodified | `test/task-2284-catalog-round-trip.test.ts:159-181`, `"round trip reports zero field differences for real task records copied from all three stores"`, `git status --porcelain backlog/` (no output) | PASS |
| 10. Follow-up tasks exist with compatibility and rollback acceptance criteria; each id absent from all three stores on current `main` | `backlog/tasks/task-2315 - Implement-byte-faithful-task-record-import-and-export-for-the-SQLite-task-catalog.md:49` (compatibility), `:50` (rollback); `backlog/tasks/task-2316 - Cut-over-task-catalog-authority-to-SQLite-and-retire-Markdown-write-paths.md:48` (compatibility), `:49` (rollback); `git ls-tree -r --name-only origin/main -- backlog/` returns no `task-2315` or `task-2316` | PASS |
| 11. Mission diff contains no `src/adapters/sqlite/migrations/` path, no `backlog/config.yml`, and no deletion or rename under `backlog/` | `git diff --name-only $(git merge-base HEAD main) HEAD` — 13 paths, none matching those patterns; `git diff --name-status --diff-filter=DR $(git merge-base HEAD main) HEAD -- backlog/` returns zero entries | PASS |
| 12. ADR 0044 and ADR 0051 each reference ADR 0052 as owner of the task-catalog authority decision | `docs/adr/0044-workflow-distribution-model.md:169`, `docs/adr/0051-ui-neutral-application-boundary.md:46`, `docs/adr/0051-ui-neutral-application-boundary.md:5` | PASS |
| 13. All required gates exit 0 on the final tree | `./scripts/verify-local.sh docs` (exit 0), `./scripts/verify-local.sh static-analysis` (exit 0, all four stages PASS), `./scripts/verify-local.sh all` (exit 0, 1310 tests, 0 fail), and `./scripts/verify-local.sh integrate` (exit 0) | PASS |
| Restricted areas untouched | `px review task-2284 --verify` validates the mission diff; restricted paths are `src/platform/runtime/lib/tools/backlog.ts`, `src/platform/runtime/lib/commands/integrate.ts`, `backlog/config.yml`, `prompts/`, `scripts/`, and `workflow.config.json` | PASS |
| NEL stop rule reported rather than silently exceeded | 531 NEL measured over `test/helpers/task-2284-catalog-round-trip.ts` and `test/task-2284-catalog-round-trip.test.ts` per `src/platform/runtime/lib/core/nels.ts:24-34` exclusions; disclosed above | REPORTED |

Next action: hand off for review; the reviewer should weigh the 531 NEL overrun
against the CP-2 finding that byte-identity assertion — not field comparison —
is what makes `test/task-2284-catalog-round-trip.test.ts` a valid proof, and
decide whether TASK-2315 should inherit the harness as-is.
