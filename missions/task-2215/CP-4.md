# CP-4: Gates verified, mission complete (task-2215)

## Summary

Ran both mission-declared gates on the final tree:

- `./scripts/verify-local.sh static-analysis` — ESLint clean, `npm run typecheck` clean, test-hygiene scanner clean ("ALL STAGES PASSED").
- `./scripts/verify-local.sh all` — 2118 tests, **2095 pass, 0 fail**, 23 pre-existing annotated skips (no `.only` / no bare `.skip` introduced; test-hygiene stage passed).

Also committed the rebuilt tracked compiled artifact `lib/commands/repair-handoff.js` (regenerated by `npm run build:cjs` from the CP-2 classifier fix).

Delivered changes across CP-1…CP-4:

1. Reproduction test `test/task-2215-missing-error-bounce.test.js` (red at parent tree — classifier returned `InfraBlocker`, template row cited `handoff.js`; green after the fixes).
2. `classifyError` now maps the `"even after auto-remediation"` handoff failure to `MissingArtifacts`/`AutoSendBack` (`lib/commands/repair-handoff.ts:124`), restoring the ADR 0048 error bounce.
3. `buildAutoCheckpointContent` evidence rows now cite `lib/commands/handoff.ts:262` and `lib/commands/handoff.ts:47`, passing `findUnverifiableGoalCheckRow`.
4. Regression tests added to `test/repair-handoff.test.js` and `test/handoff.test.js`; test seams `_buildAutoCheckpointContent` / `_findUnverifiableGoalCheckRow` / `_collectGoalCheckEvidenceRows` exported from `lib/commands/handoff.ts:1047`.

Docs note: no documentation change is required — the fix restores the behavior already prescribed by ADR 0048 (`docs/adr/0048-fail-closed-harness-defense-against-agent-hallucinations.md`); no workflow or user-facing behavior diverges from what is documented.

## Goal Check

| Criterion | Evidence | Status |
|---|---|---|
| SC1: auto-remediation error classifies as MissingArtifacts/AutoSendBack | lib/commands/repair-handoff.ts:124, "classifyError recognizes auto-remediation failure as MissingArtifacts", "task-2215 repro: classifyError classifies auto-remediation checkpoint failure as MissingArtifacts/AutoSendBack" | PASS |
| SC2: buildAutoCheckpointContent passes findUnverifiableGoalCheckRow | lib/commands/handoff.ts:854, "buildAutoCheckpointContent produces verifiable evidence rows", "task-2215 repro: buildAutoCheckpointContent evidence rows pass findUnverifiableGoalCheckRow validation" | PASS |
| SC3: no regression in classifier suite | `node --test test/repair-handoff.test.js` — 0 failures in `./scripts/verify-local.sh all` run; "classifyError returns InfraBlocker(HumanOnly) for unknown error" still passes | PASS |
| SC4: no regression in handoff suite | `node --test test/handoff.test.js` — 0 failures in `./scripts/verify-local.sh all` run; "performHandoff succeeds after successful agent relaunch" still passes | PASS |
| SC5: static analysis clean on changed files | `./scripts/verify-local.sh static-analysis` — ESLint, tsc typecheck, and test-hygiene all PASS | PASS |
| SC6: full verification suite passes | `./scripts/verify-local.sh all` — 2118 tests, 0 fail | PASS |
| DoD #6: red-to-green reproduction test for bug-labeled mission | test/task-2215-missing-error-bounce.test.js — red run recorded in missions/task-2215/CP-1.md:18, green in `./scripts/verify-local.sh all` | PASS |
| Error bounce aligns with prescribed dispatch | ADR 0048 | PASS |

Next action: hand off task-2215 to review — all four checkpoints are committed, both declared gates pass on the final tree, and the reproduction test is green.
