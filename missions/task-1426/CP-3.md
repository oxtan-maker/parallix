Checkpoint 3 finished the refactor by leaving `lib/agents/agents.ts` as the orchestration entrypoint over extracted helper modules, while preserving retry, reroute, blocklist, and session behavior. I also attempted the required graph refresh with `graphify update .`, but the CLI is not installed in this shell (`graphify: command not found`), so the final evidence relies on the passing repo gates and focused regression coverage instead.

## Goal Check
| Criterion | Evidence | Status |
|---|---|---|
| SC 1: `agents.ts` is orchestration-focused and config/blocklist helpers live in dedicated helper modules | `lib/agents/agent-config.ts:36`, `lib/agents/agent-config.ts:48`, `lib/agents/agent-config.ts:56`, `lib/agents/agent-config.ts:101`, `lib/agents/agent-config.ts:130`, `lib/agents/agent-config.ts:150`, `lib/agents/agent-config.ts:155`, `lib/agents/agents.ts:8` | PASS |
| SC 2: git worktree helpers moved to dedicated helper modules with warning/cache behavior preserved | `lib/agents/worktree.ts:7`, `lib/agents/worktree.ts:11`, `lib/agents/worktree.ts:23`, `lib/agents/worktree.ts:30`, `lib/agents/worktree.ts:57`, "readAgentConfig migrates blocklist from main worktree path", "readAgentConfig does not warn when main-worktree lookup runs outside a git repo" | PASS |
| SC 3: launcher registry, probing, selection, support assertion, and watchdog resolution live in dedicated helper modules | `lib/agents/launcher-selection.ts:24`, `lib/agents/launcher-selection.ts:70`, `lib/agents/launcher-selection.ts:105`, `lib/agents/launcher-selection.ts:118`, `lib/agents/launcher-selection.ts:128`, `lib/agents/launcher-selection.ts:187`, `lib/agents/launcher-selection.ts:217`, `lib/agents/launcher-selection.ts:242`, "workflowLauncherStatus rejects a launcher that exists but fails its health probe", "selectAgent weighted result always stays within the available set" | PASS |
| SC 4: `startAgent()` preserves retry, reroute, blocklist, persistence, and resume semantics after extraction | `lib/agents/agents.ts:169`, `lib/agents/agents.ts:234`, `lib/agents/agents.ts:246`, `lib/agents/agents.ts:355`, `lib/agents/agents.ts:412`, `lib/agents/agents.ts:464`, "startAgent reroutes an explicit agent override that is already in the blocklist", "startAgent reroutes when the selected agent fails its health probe", "startAgent persists a block via updateAgentBlock when limit-hit detector fires", "invalid-model launch failure retries without persisting a blocklist entry", "non-limit launch failure with transient error retries and persists a block for non-custom agents", "startAgent passes resume:true to claude when a matching marker exists" | PASS |
| SC 5: external import stability is preserved through `lib/agents/agents.ts` | `lib/agents/agents.ts:483`, `test/agents.test.js`, `test/agents-limit-hit.test.js`, "SC 6: resume-capable agents use session persistence via startAgent" | PASS |
| SC 6: verification proves the shared agent harness surface still works | `./scripts/verify-local.sh all`, `./scripts/verify-local.sh static-analysis`, `test/agents.test.js`, `test/agents-limit-hit.test.js` | PASS |

Next action: commit `missions/task-1426/CP-1.md`, `missions/task-1426/CP-2.md`, and `missions/task-1426/CP-3.md` with the refactor so handoff can proceed without the uncommitted-checkpoint blocker.
