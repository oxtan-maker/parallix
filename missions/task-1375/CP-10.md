# CP-10: Convert review-commands.js (1,471 lines)

## Work Done
- Converted `lib/review/review-commands.js` (1,471 lines) → `lib/review/review-commands.ts` with ES `import`/`export`, explicit TypeScript types derived from JSDoc
- Replaced all `require()` with ES `import` from converted sibling modules using relative `.js` extensions
- Changed lazy `require('../commands/handoff.js')` to `await import('../commands/handoff.js')` with `@ts-expect-error` suppression (handoff.js not yet converted to TS)
- Removed `lib/review/review-commands.js` from git tracking
- Negation line removed from `.gitignore` and `.eslintignore`

## Goal Check

| Criterion | Evidence |
|-----------|----------|
| review-commands.ts exists | `lib/review/review-commands.ts` — ES exports |
| No module.exports | `grep -c 'module\.exports' lib/review/review-commands.ts` — 0 matches |
| No require() | `grep -c "require(" lib/review/review-commands.ts` — 0 matches |
| tsc --noEmit clean | `npx tsc --noEmit` — zero errors |
| Tests unchanged | `npm test`: pass=1731, fail=0, skipped=22 (baseline match) |
| .js removed from git | `git ls-files lib/review/review-commands.js` — no output |

## Next action
Convert `review-loop.js` (CP 11, ~1,100 lines, most complex).
